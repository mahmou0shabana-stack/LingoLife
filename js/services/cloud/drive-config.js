/**
 * LingoLife — إعدادُ Google Drive (WS-H · بند ١٨)
 *
 * ═══════════════════════════════════════════════════════════════
 * ⚠️ **الموضعُ محجوزٌ والقيمةُ فارغة — بقرارٍ لا بنسيان**
 * ═══════════════════════════════════════════════════════════════
 *
 * كلُّ ما تحت هذا الملفّ من طبقة السحابة مبنيٌّ ومُختبَرٌ ويعمل — ضدّ
 * المحاكي. وينقصه شيءٌ واحدٌ **لا يملكه الكود**: مُعرِّفُ عميلٍ حقيقيّ
 * يُنشَأ في Google Cloud Console بحسابك أنت.
 *
 * ولذلك تُرك فارغًا:
 *
 *   · **مُعرِّفٌ وهميٌّ أسوأُ من لا شيء** — تبدو الواجهةُ قابلةً للربط،
 *     فتضغط، فيردّ Google بخطأٍ لا معنى له. والفراغُ يقول الحقيقةَ:
 *     «لسه مفيش».
 *   · وأيُّ مُعرِّفٍ يُكتَب هنا **منشورٌ للعالم** — الملفّ يُخدَم كنصٍّ
 *     من GitHub Pages. وهذا مقبولٌ لمعرِّف العميل (فهو ليس سرًّا بطبعه،
 *     ويحرسه الأصلُ المسموح)، **وممنوعٌ منعًا باتًّا** لأيّ
 *     `client_secret` أو مفتاحِ واجهة.
 *
 * ═══════════════════════════════════════════════════════════════
 * ⚠️ **ولا سرَّ في المتصفّح أصلًا — لا هنا ولا في أيّ مكان**
 * ═══════════════════════════════════════════════════════════════
 *
 * نموذجُ الرمز المزمَع (Google Identity Services · Token Model) لا يملك
 * سرَّ عميلٍ ولا رمزَ تجديد: يُعطى رمزُ وصولٍ عمرُه ساعةٌ تقريبًا، ثم
 * يُطلَب رمزٌ جديد. وذلك بالضبط سببُ حالة `AUTH_REQUIRED`: انتهاءُ
 * الإذن **حالةٌ عاديّةٌ متوقّعة** لا عطبٌ طارئ.
 *
 * ⚠️ **والنطاقُ المزمَع `drive.file` لا `appDataFolder`**: مجلّدٌ تراه
 *    بعينك في Drive وتنزّل منه بيدك، ولا تمحوه «إدارةُ التطبيقات».
 */

/**
 * مُعرِّفُ عميل Google.
 *
 * ⚠️ **وهذا ليس سرًّا — والفرقُ يجب أن يكون واضحًا.** مُعرِّفُ العميل
 *    مُعلَنٌ بطبعه: يظهر في كلّ طلبِ تفويضٍ يراه المتصفّح. والذي يحميه
 *    ليس خفاؤه بل **الأصلُ المسموح** (Authorized JavaScript origins):
 *    فلا يستطيع موقعٌ آخر أن يطلب رمزًا بهذا المعرِّف.
 *
 *    أمّا `client_secret` فسرٌّ حقيقيّ، **ولا وجودَ له هنا ولا في أيّ
 *    ملفٍّ في هذا المشروع** — ولا يحتاجه نموذجُ الرمز في المتصفّح أصلًا.
 */
export const DRIVE_CLIENT_ID =
  '321688352057-gqufhsa256qjkujd2sg2iu7c3r37jqgl.apps.googleusercontent.com';

/** النطاقُ المطلوب — أضيقُ ما يكفي، وغيرُ حسّاسٍ فلا يحتاج تدقيقًا. */
export const DRIVE_SCOPE = 'https://www.googleapis.com/auth/drive.file';

/** هل يمكن للتطبيق أن يعرض زرَّ ربطٍ يعمل؟ */
export function driveConfigured() {
  return typeof DRIVE_CLIENT_ID === 'string' && DRIVE_CLIENT_ID.length > 0;
}

/**
 * سببُ عدم الجاهزيّة — نصٌّ يُعرَض بدل زرٍّ ميّت.
 *
 * ⚠️ ولا يقول «حصل خطأ»: لم يفشل شيء. يقول ما ينقص ومن يملكه.
 */
export const NOT_CONFIGURED = Object.freeze({
  title: 'ربط Google Drive لسه مش مفعّل',
  detail: 'كل حاجة تحت الزرّ ده متبنيّة ومتختبرة، بس ناقصها مُعرِّف عميل '
    + 'من Google Cloud Console — وده بيتعمل بحسابك إنت، مش من جوّه التطبيق.',
});
