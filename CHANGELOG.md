# سجل التغييرات

كل التغييرات المهمة في LingoLife.
الصيغة مبنية على [Keep a Changelog](https://keepachangelog.com/ar/1.1.0/).

---

## [0.3.0] — النسخ الاحتياطي `.llife`

نسخة احتياطية حقيقية تُغني عن الجهاز: ملف واحد فيه بياناتك وصورك
الأصلية وتسجيلاتك وإعداداتك، يُسترجَع كاملًا على أي جهاز.

### أُضيف
- **حزمة `.llife`** — أرشيف ZIP بلا ضغط يحمل البايتات الأصلية للوسائط
  بلا إعادة ترميز. صيغة مفتوحة تُقرأ بأي أداة zip لو اختفى التطبيق.
- **صيغة نسخة مستقلّة عن شكل قاعدة البيانات**، بإصدار خاص بها
  (`BACKUP_FORMAT_VERSION`) وطبقتَي ترجمة. القاعدة تتغيّر والنسخ القديمة
  تبقى قابلة للاسترجاع.
- **سلسلة ترقيات باتجاه واحد** للنسخ القديمة، مع مستورد وحيد يعرف
  الصيغة الحالية فقط.
- **فحص ثلاثي المستويات** قبل الاسترجاع (بنيوي · مرجعي · سلامة) بتقرير
  يُعرض قبل لمس أي بيانات. العطب الجزئي تنبيه لا رفض.
- **استرجاع ذرّي بخانتين ومؤشّر** — إمّا أن ينجح كاملًا أو تبقى بياناتك
  القديمة كما هي. لا نصف استرجاع.
- **حفظ ثلاثي الطبقات**: شاشة المشاركة (الطريق الوحيد المباشر إلى Drive
  من تابلت) ثم منتقي المكان ثم التنزيل.
- CRC-32 لكل عنصر (مجاني من صيغة ZIP) و SHA-256 في الـ manifest.
- دعم ZIP64 — بدونه كانت النسخة تتلف صامتةً فوق 4GB.
- **30 اختبارًا** تعمل على IndexedDB وBlobs حقيقية في متصفح حقيقي،
  وملفان ذهبيان في `tests/fixtures/` يحرسان التوافق الخلفي.

### أُصلح
- **`SCHEMA_VERSION` كان متقادمًا** — ثابتًا مكتوبًا بيدٍ بقيمة 1 بينما
  القاعدة تعمل على v2، فكان كل تصدير يختم نفسه بإصدار خاطئ. أُزيل
  الثابت، والمصدر الوحيد الآن مشتقّ من الترقيات فلا يمكن أن يتقادم.
- `.gitignore` كان سيبتلع الملفات الذهبية مع `*.llife`.

### تغيّر
- التصدير النصّي (JSON) صار موسومًا صراحةً بأنه **ليس نسخة احتياطية**
  وانتقل خلف قسم قابل للطي.

راجع [docs/07-backup-format.md](docs/07-backup-format.md) للمواصفة الكاملة.

---

## [0.2.0] — المرحلة 1أ: لوحة الذكرى

### التصميم
أُعيدت اللغة البصرية بالكامل لتطابق صور التصميم: خلفية كريمية، بطاقات بيضاء،
لمسة بنفسجية، وشريط جانبي على التابلت وسطح المكتب من عرض 768px.

**المبدأ الحاكم:** كل قسم حاضر في مكانه من أول لحظة. القسم الفاضي خانة جاهزة
للإضافة — لا صناديق "قريبًا".

### أُضيف
- **لوحة الذكرى** بتسعة أقسام: اللحظة · الصور · الأصوات · السكريبتات ·
  المحادثة · خطأ/طبيعي · اللغة · رحلة التعبير · احكيها · ملاحظاتي
- فهرس مصغّر لاصق بأعداد حقيقية لكل قسم
- **الصور:** رفع متعدد، الأصل بلا ضغط، مصغّرة WebP منفصلة، موزاييك،
  شارة الغلاف، عارض بتعيين غلاف وتنزيل الأصل وإزالة بتراجع
- **الأصوات:** رفع + تسجيل بـ MediaRecorder (Opus ~0.35 ميجا/دقيقة) + تشغيل
- **السكريبتات:** إضافة، تبويبات، تعديل ينشئ نسخة في التاريخ، نسخ، تعيين أساسي
- **المحادثة:** أجزاء بفقاعات تفرّق بين كلامي وكلام الطرف التاني
- **خطأ / طبيعي:** أحمر مقابل أخضر + نوع الخطأ + الشرح بالمصري
- **التعبيرات:** كيانات عالمية — التكرار يُربط كظهور جديد لا كنسخة
- الملاحظات الشخصية بحفظ تلقائي عند مغادرة الحقل
- شاشة "دلوقتي" الجديدة بهيرو كبير وبطاقات الاسترجاع

### schema
- **v2:** فهرس `sceneId` على `conversationParts` — طُبِّق على بيانات موجودة
  بلا فقدان أي سجل

### إصلاحات
- `backdrop-filter` على الهيدر كان يجعله الحاوي المرجعي للعناصر الثابتة داخله،
  فيرمي شريط التنقّل السفلي إلى أعلى الشاشة على الهاتف. أُزيل.
- `responsive.css` كان يحمل نسخة قديمة من قواعد الشريط الجانبي تتغلّب على
  `layout.css` وتكسر التخطيط. أُعيدت كتابته ليقتصر على تعديلات المقاسات الصغيرة.
- نصوص الخانات الفاضية كانت تلتصق ببعضها (عناصر inline) — صارت block.

---

## [0.1.0] — المرحلة 0: الأساس

### أُضيف

**طبقة البيانات**
- IndexedDB بـ 30 store مُطبّعة، schema v1
- نظام ترقيات غير مدمّر للأمام فقط (`js/db/migrations.js`)
- طبقة repositories موحّدة: ختم تلقائي لـ `id/createdAt/updatedAt/rev/state/dirty`
- ثلاث حالات لكل سجل: `active` / `archived` / `trashed`
- معرّفات ULID مرتّبة زمنيًا ببادئة لكل كيان

**التطبيق**
- Hash router يعمل تحت مسار فرعي بلا إعداد خادم
- شاشات: دلوقتي، حياتي، لغتي، المشهد، الإعدادات، سلة المهملات
- إنشاء ذكرى بعنوان عربي وروسي وتاريخ ونوع ومكان وسياق
- خط زمني مجمّع بالشهر
- نقل للسلة مع **تراجع**، استرجاع، وحذف نهائي بتأكيد يشرح العواقب

**PWA**
- `manifest.webmanifest` + أيقونات (192/512/maskable/SVG)
- Service Worker: network-first للكود، cache-first للأصول
- شريط "تحديث جاهز" — لا إعادة تحميل صامتة أثناء الكتابة
- عمل كامل بلا اتصال بعد أول تحميل

**التخزين والأمان**
- `navigator.storage.persist()` تلقائيًا عند أول تشغيل
- شاشة صحة تخزين بأرقام حقيقية من `estimate()` وعدّ فعلي
- تصدير JSON لكل البيانات النصية
- زر "تحديث الكود بالقوة" يمسح الكاش **دون لمس IndexedDB**

**البنية**
- لغة بصرية في `tokens.css` — لا لون أو مسافة مكتوبة مباشرة في أي ملف آخر
- عزل اتجاه اللغة: عربي RTL وروسي LTR في نفس الصفحة
- قالب `html` موسوم يهرّب المدخلات تلقائيًا
- تطبيع بحث للعربية (تشكيل، أ/ا، ة/ه) والروسية (ё/е)
- GitHub Action ينشر على Pages ويختم رقم البناء

### مُثبَّت باختبار
- الذكرى نجت من 3 عمليات نشر وهمية + إغلاق كامل للتبويب
- الكاشات القديمة تُحذف والجديد وحده يبقى

### إصلاحات أثناء البناء
- `clients.claim()` عند أول تثبيت كان يطلق `controllerchange` فيعيد تحميل
  الصفحة بلا سبب — وكان ممكنًا أن يضيّع نموذجًا نصف مملوء. الآن إعادة التحميل
  لا تحدث إلا بعد ضغط المستخدم على "حدّث".
- عرض أحجام التخزين كان ينقلب بصريًا في سياق RTL — أُصلح بـ `<bdi>`.

### معروف وغير منفَّذ
راجع [STATUS.md](STATUS.md).
