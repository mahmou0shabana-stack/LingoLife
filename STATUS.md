# حالة التنفيذ

> تُحدَّث بعد كل مرحلة. الصدق قبل التجميل — لا زر يبدو أنه يعمل وهو لا يعمل (بند 89).

**آخر تحديث:** كتاب الظلّ — كل مزايا التطبيق القديم · إصدار 0.6.0 · schema v3 ·
صيغة النسخة v1

> **مبدأ التصميم:** كل قسم من أقسام الذكرى حاضر في مكانه من أول لحظة.
> القسم الفاضي يظهر كخانة جاهزة للإضافة — لا صناديق "قريبًا" ولا أزرار شكل.

---

## ✅ يعمل — مُختبَر فعليًا

| الميزة | ملاحظة الاختبار |
|---|---|
| فتح IndexedDB وتشغيل الترقيات | 30 store أُنشئت من v0 → v1 |
| إنشاء ذكرى (عنوان عربي/روسي، تاريخ، نوع، مكان، سياق) | مشهد حقيقي أُنشئ وحُفظ |
| كتلة Raw Transcript تُنشأ مع المشهد | `contentBlocks.kind = rawTranscript` |
| صفحة الذكرى بأرقام حقيقية | الصور/الأصوات/السكريبتات = 0 وهذا صحيح |
| الخط الزمني مجمّعًا بالشهر | يعمل |
| التنقّل (دلوقتي / حياتي / لغتي / مشهد / إعدادات / سلة) | hash routing يعمل |
| نقل للسلة + **تراجع** من الـ toast | يعمل |
| استرجاع من السلة + حذف نهائي بتأكيد | يعمل |
| **بقاء البيانات عبر تحديثات الكود** | ✅ 3 عمليات نشر وهمية + إغلاق التبويب بالكامل → الذكرى باقية |
| Service Worker + حذف الكاشات القديمة | بقي `lingolife-<build>` وحده |
| شريط "تحديث جاهز" بدل إعادة تحميل صامتة | ظهر وعمل |
| صحة التخزين بأرقام حقيقية | `estimate()` + عدّ فعلي لكل store |
| تصدير JSON | 27 store، صيغة صحيحة، بيانات سليمة |
| RTL عربي + LTR روسي في نفس الصفحة | مؤكَّد بصريًا |
| العمل بلا اتصال بعد أول تحميل | App Shell مخزَّن مسبقًا |
| **لوحة الذكرى بكل أقسامها** | 9 أقسام في أماكنها، الفاضي منها خانة قابلة للضغط |
| **رفع صور متعدد** | 3 صور اترفعت، الأصل بلا ضغط + مصغّرة WebP منفصلة |
| موزاييك الصور + شارة الغلاف | يعمل |
| عارض الصورة: غلاف / تنزيل الأصل / إزالة بتراجع | يعمل |
| **إضافة سكريبت + تبويبات + نسخة في التاريخ** | يعمل |
| نسخ السكريبت / تعيينه أساسيًا | يعمل |
| **أجزاء المحادثة بفقاعات** | يعمل — أنا مقابل الطرف التاني |
| **خطأ / طبيعي** بالأحمر والأخضر + الشرح | يعمل |
| **التعبيرات** ككيانات عالمية + تسجيل الظهور | يعمل — التكرار بيتربط مش بيتعمل نسخة |
| الملاحظات الشخصية (حفظ تلقائي) | يعمل |
| الشريط الجانبي على التابلت وسطح المكتب | يعمل من 768px |
| ترقية schema من v1 إلى v2 على بيانات موجودة | ✅ بلا فقدان سجل |
| **حزمة `.llife` كاملة بالصور والأصوات الأصلية** | ✅ 30 اختبارًا في متصفح حقيقي |
| أرشيف ZIP صالح بأدوات النظام | ✅ `unzip -t` → No errors detected |
| البايتات الأصلية تعود مطابقة تمامًا | ✅ مقارنة بايت ببايت بعد الاسترجاع |
| الحفظ عبر شاشة المشاركة / منتقي المكان / التنزيل | ✅ الطبقات الثلاث مع السقوط بينها |
| الفحص ثلاثي المستويات قبل الاسترجاع | ✅ يكشف المفقود والتالف واليتيم وعدم تطابق الأعداد |
| **الاسترجاع الذرّي — فشله لا يمسّ البيانات القديمة** | ✅ مُختبَر بإفشال متعمّد في المنتصف |
| استرجاع ملف ذهبي بصيغة v1 على قاعدة اليوم | ✅ ملفان في `tests/fixtures/` |
| إصلاح `SCHEMA_VERSION` المتقادم | ✅ كان يختم كل تصدير بـ v1 والقاعدة على v2 |
| **سكريبت ← ظلّ بلا نسخ ولصق** | ✅ 5 جمل اتقسمت واتفتحت في الكتاب |
| تقسيم الجمل والكلمات بمنطق التطبيق القديم | ✅ مُختبَر بحالاته الحدّية |
| محرّك التكرار برمز الدورة | ✅ اختبار صريح يمنع تراكب الجمل |
| الكتاب المنقسم أفقيًا (تابلت) ورأسيًا (موبايل) | ✅ `row` على 1024px و`column` على 420px |
| إبراز الجملة الجارية في الصفحتين معًا | ✅ |
| استئناف الموضع بعد إعادة التحميل | ✅ رجع على 4/5 |
| كشف تغيّر المصدر بلا استبدال صامت | ✅ |
| **دليل ممارسة بلا ترقية كاذبة للإتقان** | ✅ 50 تكرار ما رفعتش حاجة |
| **الوضع الغامر بالثيم الداكن** | ✅ الثيم محصور في `.shadow-app` — باقي التطبيق كريمي |
| أقراص السرعة والتكرار والتوقّف | ✅ 0.8x→0.7x و ×5→×7 بالضغط |
| أوضاع العرض: RU / مصري / مخفي | ✅ المخفي بيتكشف بالنقر |
| وضع التكرار: بالعدد / مستمرّ | ✅ |
| مستوى الصوت واختيار الصوت الروسي | ✅ |
| سجّل الآن — يحفظ نطقك في المشهد | ✅ بدور `pronunciation` |
| **علامات النبر (ударение)** | ✅ قاموس محلّي 80 كلمة + قاموسك المتراكم |
| **عشرة خطوط للعرض** | ✅ زرّ واحد يدوّر بينها |
| **سجلّ آخر 20 جملة** | ✅ مبنيّ على دليل ممارسة حقيقي لا localStorage |
| **الترجمة لـ15 لغة** | ✅ اختيارية ومطفية افتراضيًا |
| شعلة الأيام المتتالية | ✅ محسوبة من `practiceEvidence` لا عدّاد يدوي |
| الطابع الزمني على كل جملة | 🟡 تقدير من طول النصّ لا قياس من صوت |

---

## 🟡 جزئي — يعمل بحدود مذكورة

| الميزة | الحد |
|---|---|
| **التصدير النصّي (JSON)** | بيانات نصية فقط بلا وسائط — للمعاينة السريعة، **وليس نسخة احتياطية**. البديل الكامل هو `.llife` |
| **الاسترجاع** | «استبدال الكل» فقط. الدمج مؤجَّل — قيود `unique` تجعله دمج كيانات حقيقية (docs/07 §7.8) |
| **SHA-256 للوسائط** | تُتخطّى للملفات فوق 256MB (`crypto.subtle` لا يدعم التجزئة على دفعات). CRC-32 يغطّيها ويُحسب على قطع |
| **ذروة المساحة عند الاسترجاع** | ضعف حجم بياناتك لحظيًا — ثمن الذرّية المعلن (docs/07 §7.8) |
| **البحث** | `searchScenes()` موجودة وتعمل على العناوين والسياق والمكان، لكن **لا توجد شاشة بحث بعد** |
| **التخزين الدائم** | يُطلب تلقائيًا. المتصفح قد يرفضه قبل تثبيت التطبيق على الشاشة الرئيسية |
| **الأماكن** | `resolvePlace()` مكتوبة وجاهزة لكن غير موصولة — المكان يُخزَّن نصًا الآن |
| **التسجيل الصوتي** | يعمل عبر MediaRecorder بصيغة Opus. يحتاج إذن الميكروفون و HTTPS (أو localhost) |
| **رحلة التعبير** | الخط الزمني يعرض المرحلة الحالية فقط. التواريخ والانتقالات الفعلية في المرحلة 2 |
| **تشغيل الصوت** | تشغيل بسيط. السرعة والتكرار والـ Loop في المرحلة 1ب |
| **زر تصدير الذكرى** | ينسخ العنوان والسياق للحافظة. تصدير `.llife` كامل في المرحلة 1ب |
| **الظلّ** | من السكريبت فقط. المحادثة والتحديد لسه — راجع docs/08 §8.7 |
| **استخراج النصّ من صورة (OCR)** | الكود جاهز بمصدرين (محلّي ثم CDN)، لكن ملفات Tesseract (~20MB) لم تُضمّ بعد. شغّل `scripts/vendor-tesseract.sh` |
| **الطابع الزمني** | تقدير من عدد الكلمات — التطبيق ينطق بـ TTS فمفيش شريط صوتي يُقاس عليه |
| **الترجمة في الظلّ** | تُعرض لو محفوظة في المقطع. الترجمة التلقائية مستبعَدة عمدًا (APIs خارجية) |

---

## ⬜ غير منفَّذ — المرحلة القادمة

**المرحلة 1 (المشهد الكامل):** الصور، الأصوات، التسجيل، السكريبتات، المحادثة،
Before/Natural، Focus View، Journal Spread للتابلت، حزمة `.llife`، شاشة البحث.

**المرحلة 2 (عالم اللغة):** التعبيرات ككيانات، Expression Life، الكلمات، الأنماط،
الأرقام القابلة للنقر.

**المرحلة 3 (Drive):** OAuth، Workspace، المزامنة، الاسترجاع من السحابة.

**المرحلة 4:** الأشخاص، الأماكن، Time Machine، المراجعة الحيّة بجدولة SRS.

**المرحلة 5:** Living Analysis (بلا أي نتائج وهمية).

**المرحلة 6:** التحليلات، ترتيب الأقسام، ثيمات المشهد، مراجعة الإتاحة.

---

## معايير قبول المرحلة 0

| # | المعيار | النتيجة |
|---|---|---|
| 1 | التطبيق يعمل عبر خادم HTTP محلي | ✅ |
| 2 | يعمل بلا اتصال بعد التخزين المسبق | ✅ |
| 3 | اتجاه العربية صحيح | ✅ |
| 4 | اتجاه الروسية صحيح | ✅ |
| 5 | البيانات تبقى بعد إعادة تشغيل المتصفح | ✅ |
| 6 | **نشر كود جديد لا يمسّ البيانات** | ✅ 3 مرات |
| 7 | لا أخطاء console في المسارات المختبَرة | ✅ |
| 8 | التنقّل لا يغطّي المحتوى | ✅ |
| 9 | لا أزرار وهمية | ✅ كل غير المنفَّذ موسوم صراحةً |
| 10 | لا إحصاءات وهمية | ✅ كل رقم مقروء من القاعدة |

---

## مشاكل معروفة

1. **`navigator.storage.persist()` يُرفض على localhost** — سلوك متوقع. بعد النشر
   على HTTPS وتثبيت التطبيق تزيد فرصة القبول.
2. **الخطوط من Google Fonts** — أول تحميل يحتاج إنترنت. بعدها تُخزَّن في الكاش.
   التطبيق يعمل بخطوط النظام لو تعذّر تحميلها.
