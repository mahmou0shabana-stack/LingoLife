# 9 — خطة الترقية الكبرى: التدقيق وترتيب البناء

> وثيقة تخطيط لا سجلّ إنجاز. تُحدَّث كلما تغيّر ما نعرفه عن الكود.
> الحالة المُنجَزة مكانها `STATUS.md`.

---

## 9.0 لماذا هذه الوثيقة

وصلت مواصفة ترقية من 115 بندًا وثمانية workstreams. وهي — بنصّها —
**ليست قائمة مهامّ تُنفَّذ بالترتيب**، بل مواصفة معماريّة يُشتقّ منها
ترتيبُ بناءٍ يراعي الاعتماديات.

الخطر الحقيقي فيها ليس حجمها، بل أن تُنفَّذ بترتيبها الظاهر: أن يُبنى
«أطلس الذاكرة» (بند 58) فوق أنواعٍ مخزَّنة في `settings`، ثم يُعاد
بناؤه كلّه حين تصير الأنواع كيانات. **الترتيب هنا هو المضمون.**

هذه الوثيقة تُجيب عن ثلاثة أسئلة: ما الموجود فعلًا؟ ما الناقص؟ وبأي
ترتيب يُبنى الناقص حتى لا يُبنى شيءٌ مرّتين؟

---

## 9.1 نتيجة التدقيق

مصنَّفة بالحروف التي طلبتها المواصفة (بند 1).

### أ — يعمل ومُختبَر

| المجال | الدليل |
|---|---|
| IndexedDB + ترقيات أمامية (v9) | 328 اختبارًا في متصفّح حقيقي |
| المشهد: صور/أصوات/سكريبتات/محادثة/أخطاء/تعبيرات | `scene-view.js` + `content-service.js` |
| صيغة `.llife` كاملة بالوسائط الأصلية | استرجاع ذرّي بقاعدتين، ملفات ذهبية |
| محرّك الظلّ: تقسيم، تكرار، أوضاع، نبر، ترجمة | `services/shadow/*` + `shadow.test.js` |
| الصوت يكمل بعد مغادرة الشاشة | `audio-service.js` + Media Session |
| ربط الوسائط بالنصوص وببعضها | `link-service.js` فوق `relationships` |
| الأنواع ككيان في القاعدة: تفريع، نقل، ترتيب، أسماء بديلة، تعارض، تشابه، دمج | `type-service.js` + `types.test.js` |
| المحفوظات بتصنيفاتها | `saved-service.js` + `saved.test.js` |
| لوحة الصورة: تمرير داخلي، تثبيت، تحجيم | مُتحقَّق بصريًّا |
| درج الإعدادات بقيم حرّة | منزلق + خانة رقم على نفس القيمة |

### ب — منفَّذ ويحتاج تحقّقًا

| البند | ما يلزم التحقّق منه |
|---|---|
| 17 — أنماط الكتابة الروسية | **الخطوط لا تُحمَّل في بيئة التطوير** (البروكسي يحجب `fonts.googleapis.com`)، فتغطية الحروف السيريلية لكل خطّ **غير مُتحقَّق منها**. لا بدّ من قياسها على شبكة حقيقية: خطٌّ لا يغطّي السيريلية يسقط صامتًا لخطّ النظام، فتظهر «عشرة أنماط» وهي في الحقيقة أقلّ. |
| 20 — عطل وضع الكلمة | أُصلح ومعه ثلاثة اختبارات، لكن **جملة المواصفة بعينها** (`После того как документ все подпишут`) ليست حالةَ اختبار. تُضاف. |
| 7 — الكتاب المتجاوب | يتحوّل إلى صفحتين عند 900px. التابلت الرأسي (768–899) يقع على تخطيط الموبايل. |

### ج — منفَّذ جزئيًّا

| البند | الموجود | الناقص |
|---|---|---|
| 13–14 — منفذ المصدر | صورة: ملاءمة/تكبير/تحريك/تثبيت/تحجيم | السكريبت والمحادثة والنصّ المحدَّد لا تظهر في اللوحة اليسرى كمصادر |
| 21 — اختيار المقاطع | اختيار الجمل **قبل** بدء الجلسة | لا اختيار داخل الجلسة، لا «كل/صعب/غير مُتدرَّب»، لا عدّاد `7 / 18` |
| 19 — الممارسة السريعة | إدخال نصّ خارجي وقراءته ومسحه | لا «احفظ في الذكرى / لغتي / كتعبير» |
| 22 — النطق البشري | تسجيلك أنت المربوط بالسكريبت | مصدر الناطقين الأصليين (بند 22 يقصده تحديدًا) |
| 80–84 — الصفحة الكونية | كتاب داكن بصفحتين | الوجهان بنفسجيّان داكنان معًا؛ المواصفة تريد **يسرى دافئة/فاتحة ويمنى كونية** |
| 103–106 — النسخ والأداء | `.llife` كاملة، صحّة التخزين | لا تحميل تدريجي ولا ترقيم صفحات في «حياتي» (سقف 200 ذكرى صامت) |

### د — معطوب أو غير آمن معماريًّا

| # | المشكلة | الأثر |
|---|---|---|
| **د-1** | **السلة تعرض المشاهد وحدها.** أُضيف في الجولة الأخيرة حذفٌ بالسلة للسكريبت وجزء المحادثة والتصحيح والمحفوظة — لكن `trash-view` يقرأ `listTrashed()` وهي `scenes` فقط. | بعد انتهاء إشعار «تراجع» (ستّ ثوانٍ) **يصير العنصر غير قابل للوصول نهائيًّا**: موجود في القاعدة، غائب عن كل شاشة. هذا خرقٌ مباشر للبند 12، ونقصٌ أدخلتُه أنا. |
| د-2 | `searchScenes` و`listTrashed` تقرآن `scenes.getAll()` | مسحٌ كامل عند كل بحث. البند 105 يتوقّع 1000+ ذكرى. |
| د-3 | البحث غير موصول بمسار | `router.js` يوثّق `#/search` ولا مسار مسجَّلًا له. الدالّة موجودة ولا تُنادى. |
| د-4 | `app.js` بلغ 1329 سطرًا | يحمل التوجيه وكل النوافذ وعارض الصور ومداخل الظلّ والربط. WS1 يضيف إليه أضعافه. |
| ~~د-5~~ ✅ | ~~`relationships` بحقلين لنفس المعنى~~ التأم في v8: `kind` هو الحقل، و`type` أثرٌ مهجور لا يُكتب ولا يُحذف. |

### هـ — غائب تمامًا

~~بندان 24–31 (خيوط الأحداث)~~ ✅ · 32–37 (استيراد مشهد مُجهَّز) · 38–40
(اللغة المنطوقة الحقيقية والمنشأ) · ~~41–44 (الأشخاص)~~ ✅ · 51–57
(مكتبة الأصوات وطابور التشغيل) · 58–75 (أطلس الذاكرة ونهر الزمن ويوم كامل) ·
86–93 (التحليل الحيّ) · 94–102 (Google Drive).

### و — ترقيات القاعدة اللازمة

| الإصدار | المحتوى | الخطر |
|---|---|---|
| ~~v6~~ | ~~`nativeAudio`~~ ✅ مُنجَز — store واحد بمفتاح الكلمة | منخفض (إضافة محضة، ولا يدخل النسخة) |
| ~~v7~~ | ~~`eventTypes` كـ store + `scene.eventTypeId`~~ ✅ مُنجَز | كان الأعلى في المشروع — أوّل ترقية تنقل معنًى قائمًا. راجع `docs/03 §3.6` |
| ~~v8~~ | ~~فهارس `kind` على العلاقات + ردم `kind` من `type`~~ ✅ مُنجَز | منخفض (فهارس + ردم، بلا حذف) |
| ~~v9~~ | ~~`eventThreads`~~ ✅ مُنجَز — store واحد، ولا حقل على `scenes` | منخفض (إضافة محضة) |
| ~~v10~~ | ~~منشأ الظهور: `'manual'` الثابت يصير `'unknown'`~~ ✅ مُنجَز | منخفض — الحقل كان ثابتًا فلا معلومة تضيع، واختبارٌ يمنع أن تصير عمياء |
| v11 *(مؤجَّل)* | `projects` — ⏸️ قرار منتَج مؤجَّل، راجع §9.4-د | منخفض متى لزم (إضافة محضة) |

### ز — واجهات قابلة لإعادة الاستعمال

`components/type-manager.js` هو **قالب كل مدير كيانات قادم** (أشخاص،
مشاريع، تصنيفات أصوات): صفوف + تعديل داخل السطر + أرشفة + دمج + عدّ
استعمال. · `components/modal.js` صار مكدَّسًا فيحتمل التداخل. ·
`services/delete-service.js` بوّابة الحذف الواحدة. · `link-service.js`
يستوعب أي علاقة جديدة بلا جدول جديد. · `utils/normalization.js` أساس
كشف التعارض والتشابه.

### ح — منطق الظلّ القديم الواجب حفظه

استُخرج بالفعل: التقسيم، سلّم السرعة، فجوة 60ms، رمز الدورة. **وما لم
يُستخرج بعد:** مصدر النطق البشري الأصلي — وهو في الملفّ القديم عند
`_wpGetAllAudio`: نداءٌ واحد مُجمَّع لـ Wikimedia Commons بأسماء ملفّات
متوقَّعة (`Ru-<كلمة>.ogg`…)، ثم إثراءٌ في الخلفية من Lingua Libre
(`LL-Q7737 rus`) ومن `openrussian.org`، ثم منتقي أصوات يدوّر بين
الناطقين، وسقوطٌ إلى TTS عند أي فشل. كلّها `origin=*` فتعمل من
المتصفّح مباشرة. **للكلمات المفردة فقط** — الجمل تذهب لـ TTS.

### ط — ما يتشارك نفس الأساس

- الأنواع + الأشخاص + المشاريع + الخيوط + تصنيفات الأصوات = **نفس نمط
  «كيان بمعرّف ثابت واسم للعرض»**. تُبنى مرّة كخدمة عامّة لا خمس مرّات.
- الخيوط + العلاقات المُصنَّفة + اقتراحات الربط = نفس جدول
  `relationships`.
- الأطلس + البحث + الفلاتر + صفحة الخيط = **نفس طبقة الاستعلام**. بناء
  الأطلس قبل أن تصير الأنواع والأشخاص كيانات يعني بناءه مرّتين.

### ي — ترتيب البناء الذي يمنع الازدواج

1. **إصلاح ما ينزف الآن** (السلة، البحث) — لا يعتمد على شيء.
2. **الكيانات المركزية** (أنواع، أشخاص، مشاريع) — يعتمد عليها كل ما بعدها.
3. **الخيوط والعلاقات المُصنَّفة** — فوق الكيانات.
4. *(لاحقًا)* الأطلس ← يقرأ 2 و3. الاستيراد ← يكتب في 2 و3. التحليل ←
   يقترح على 2 و3. Drive ← يزامن 2 و3.

**النتيجة العمليّة:** كل ما في المستوى 4 مؤجَّل حتى يستقرّ 2 و3.

---

## 9.2 نطاق هذه الجولة

**WS0 + WS1.** وقرارات ثلاثة اتُّخذت:

| القرار | المُعتمَد |
|---|---|
| النطق البشري الأصلي | **مطفأ افتراضيًّا**، يُفعَّل بموافقة صريحة تشرح أن الكلمة تخرج لخادم خارجي — نفس سياسة الترجمة الأونلاين |
| اتجاه الكتاب | **يسرى دافئة/فاتحة، يمنى كونية** (بندان 80 و109) |
| ما بعدهما | WS2 ثم WS4 (§9.5 و§9.6)؛ الباقي مؤجَّل بترتيبه في §9.1-ي |

---

## 9.3 WS0 — إصلاحات التدقيق

### 0-أ · السلة تستوعب كل ما يُحذف — ✅ **مُنجَز**

كانت `listTrashed()` تقرأ `scenes` وحدها بينما التطبيق ينقل سبعة أنواع
إلى السلة.

صار `trash-service.js` مبنيًّا على **سجلّ واحد** (`TRASHABLE`) هو مصدر
الحقيقة: منه تُبنى الشاشة، وعليه تُختبَر التغطية. ثمانية أنواع
مسجَّلة: ذكرى · سكريبت · جزء محادثة · تصحيح · تعبير · ظهور تعبير في
ذكرى · محفوظة · صورة أو تسجيل.

ما استقرّ عليه التصميم:

- **العنصر المعروض ليس دائمًا السجلّ المحذوف.** إزالة صورة تنقل
  *الرابط* للسلة ويبقى الملفّ سليمًا؛ الصفّ يُبنى من الرابط ويُعرَض
  بالصورة، لأن ما تتذكّره أنت هو الصورة.
- **الاستعادة تُعيد العلاقة لا السجلّ وحده.** سكريبتٌ يعود بينما ذكراه
  في السلة يعود إلى الاختفاء نفسه — تُكشف السلسلة ويُعرَض القرار.
  وظهور تعبيرٍ يُعيد معه تعبيره المؤرشف، وإلا بقي محجوبًا.
- **صفٌّ لا نعرف كيف نعرضه يظهر ولا يُبتلَع.** سجلٌّ ناقص المفاتيح كان
  يُسقِط `listTrash` كلها فتختفي السلة بأكملها — وهو نقيض غرضها.
- **الأنواع المؤرشفة لا تدخل السلة**: الأرشفة قرارٌ آخر غير الحذف.
- الحذف النهائي يعرض ما يُفقَد معه بالاسم والعدد قبل السؤال (بند 12).

**الحارس:** `findStrandedTrash` + اختبارٌ يزرع محذوفًا في **كل** مستودع
في التطبيق ويطالب بأن يكون كلٌّ منه إمّا ظاهرًا في السلة وإمّا مُقَرًّا
باستثنائه في `NOT_TRASHABLE` بسببٍ مكتوب. أُثبتت فاعليّته بحذف نوعٍ من
السجلّ عمدًا: سقط الاختباران البنيويّان معًا.

**ملفّات:** `services/trash-service.js` (جديد) · `views/trash-view.js`
(أُعيد بناؤه) · `css/components.css` · `tests/trash.test.js` (جديد،
17 اختبارًا)

### 0-ب · البحث: مسار حقيقي وفهارس — ✅ **مُنجَز**

كان المسار موثّقًا في `router.js` وغير مسجَّل، والدالّة مكتوبة ولا
تُنادى: ميزةٌ كاملة بلا باب. وكانت تقرأ `scenes.getAll()` عند كل ضغطة.

صار البحث **عبر خمسة مجالات**: الذكريات · السكريبتات · التعبيرات ·
أجزاء المحادثة · المحفوظات. تكتب كلمةً روسية فتجدها في سكريبت، وفي
جملةٍ قالها أحدهم، وفي تعبير تعلّمته، وفي جملةٍ حفظتها — لأنها كلها
مكان واحد في حياتك لا خمسة.

- مؤشّر يقف عند أول `limit` مطابقة بدل تحميل الـstore في الذاكرة.
  والمشاهد تُمسح بترتيب التاريخ تنازليًّا، فما تبحث عنه غالبًا قريب.
- `limit + 1` عمدًا: الصفّ الزائد لا يُعرض وإنما يُخبرنا أن هناك المزيد.
- تصفية بمجموعة واحدة، ومقتطف يُظهر **لماذا** طابق السطر.
- `listTrashed` انتقلت إلى فهرس `state` بدل المسح الكامل.

**لماذا لا فهرس معكوس؟** `searchIndex` معرَّف في الـschema منذ البداية
ولم يُنشأ قط. بناؤه يعني ترقيةً، ومعالجًا على **كل** مسار كتابة، وردمًا
للقائم، ثم عبئًا دائمًا على كل حفظ. والمؤشّر يكفي اليوم. يُعاد النظر
حين يصير البحث بطيئًا محسوسًا على الجهاز الفعلي — لا قبل ذلك.

**ملفّات:** `services/search-service.js` (جديد) · `views/search-view.js`
(جديد) · `scene-service.js` · `app.js` · `index.html` ·
`tests/search.test.js` (جديد، 17 اختبارًا)

### 0-ج · «حياتي»: تحميل تدريجي — ✅ **مُنجَز**

كان السقف 200 ذكرى **صامتًا**: تفتح الشاشة فترى مئتين ولا شيء يقول إن
وراءها المزيد. صار 40 في الدفعة، والباقي يأتي وأنت تنزل
(`IntersectionObserver` بهامش 300px فيصل قبل أن تبلغ القاع).

والعدّاد صار من `countScenes()` — عدد ذكرياتك الحقيقي لا طول المعروض.
والشهر الذي بدأ في دفعةٍ يكمل فيها التي بعدها بدل أن يتكرّر عنوانه،
وعدّاده يزيد معه.

### 0-د · تفكيك `app.js` — ✅ **مُنجَز**

كان 1329 سطرًا يحمل التوجيه وكل النوافذ والعارض ومداخل الظلّ والربط،
فصارت كل إضافةٍ تمرّ من عنق واحد. صار **467 سطرًا**: التمهيد والتوجيه
وتفويض الأحداث لا غير.

الوحدات الخارجة:

| الوحدة | ما فيها |
|---|---|
| `ui-state.js` | الحالة العابرة · `reloadScene` · بطاقة التخزين |
| `components/type-select.js` | منتقي نوع الحدث — يتكرّر في كل نموذج |
| `modals/scene-modals.js` | إنشاء الذكرى وتعديلها |
| `modals/content-modals.js` | السكريبت · جزء المحادثة · التصحيح · التعبير |
| `modals/media-actions.js` | إضافة الصور والأصوات والتسجيل |
| `modals/link-modal.js` | ربط الوسائط وتصنيفها |
| `components/lightbox.js` | عارض الصورة |
| `services/shadow/shadow-entry.js` | مداخل الظلّ الأربعة |

`ui-state.js` هي **الوصلة** التي جعلت التقسيم ممكنًا بلا دورات: كانت
`reloadScene` و`ui` حبيستَي `app.js` بينما يحتاجهما كل نموذج. لو بقيتا
هناك لاحتاج كل نموذجٍ أن يستورد `app.js`، واحتاج `app.js` أن يستورد كل
نموذج — وهي الدورة بعينها.

**نقلٌ بلا تغيير سلوك.** ومع ذلك انكسر ثلاثة أشياء لم يكشفها أيٌّ من
الـ146 اختبارًا: استيرادٌ ناقص (`sceneMediaLinks`)، ومسارَا استيراد
ديناميكيّين تقادَما بانتقال ملفّيهما إلى مجلّد أعمق.

فأُضيف **فحص رسم الوحدات** إلى `run-headless.mjs`: يمسح `js/` من القرص
عند كل تشغيل ويستورد كل وحدة في متصفّح حقيقي. الاختبارات تستورد
الخدمات لا الشاشات، وفحص الإقلاع يلمس شاشة واحدة — أما هذا فيغطّي
الرسم كلّه (64 وحدة اليوم، والجديد يدخل تلقائيًّا بلا قائمة تتقادم).

أُثبتت فاعليّته بكسرٍ متعمَّد: كسرتُ مسارًا فسقط الفحص، وحدّد الوحدة
المكسورة وكل ما يعتمد عليها، وأنهى العملية برمز 1.

### 0-هـ · الظلّ: ما تبقّى من التدقيق — ✅ **مُنجَز**

| # | العمل | الحال |
|---|---|---|
| 20 | جملة المواصفة بعينها كحالة اختبار: ستّ كلمات، تشغيل/سابق/تالي/تكرار/تقدّم تلقائي، **واستقلال موضع وضع الجملة عن وضع الكلمة** | ✅ |
| 7 | التابلت الرأسي يحتفظ بالكتاب (نقطة توقّف عند 768px بنسبة مختلفة) | ✅ |
| 21 | شريط اختيار داخل الجلسة: الكل · مسح · صعبة · غير مُتدرَّب · عدّاد `7 / 18` · «تدرّب على المحدَّد» | ✅ |
| 19 | من الممارسة السريعة: احفظ في المحفوظات · كتعبير · كسكريبت | ✅ |
| 17 | قياس تغطية السيريلية لكل خطّ؛ ما لا يغطّيها يُستبدَل أو يُوسَم صراحةً. وإعادة التسمية بصيغة الكتابة لا بأسماء ذوقيّة | ✅ |
| 13 | السكريبت والمحادثة والنصّ المحدَّد كمصادر ظاهرة في اللوحة اليسرى | ✅ (على مرحلتين — انظر أدناه) |

**بند 21 — الاختيار في المحرّك لا في العرض.** كان يمكن ترشيح السطور
المعروضة وحدها، لكن المحرّك حينها يكمل على المخفيّ. فصار
`setSelection(indices)` يقود `next` و`previous` والتقدّم التلقائي.
والمجموعة الفارغة تعني **الكل** — لا علم منفصل ولا حالة ثالثة، فالجلسة
بلا تحديد تسلك كما كانت حرفيًّا. والتحديد لا يُحفظ: عدسةٌ لجولةٍ، لا
خاصيّةٌ من خصائص النصّ. و«الصعبة» و«اللي لسه» تقرآن علامةً وعدّادًا
موجودين أصلًا، بلا حقلٍ جديد.

**بند 19 — الممارسة السريعة كانت تضيع.** المربّع منفصل عن الجلسة عمدًا
(فلا يلوّث دليل الممارسة)، لكن الانفصال كان يعني أن ما تكتبه يذهب مع
أول «✕». صارت له ثلاث وجهات صريحة: المحفوظات · تعبير في الذكرى ·
سكريبت جديد. والوجهتان الأخيرتان تحتاجان جلسةً مربوطة بذكرى، وحين لا
تكون **تُقال المشكلة** بدل فشلٍ صامت. والحفظ لا يمسّ نصّ الجلسة.

**بند 17 — التسمية والتغطية.** الأسماء الذوقيّة («راقص» · «حرّ» ·
«أنيق») لا تقول شيئًا عمّا سترى. صارت بصيغة الكتابة، مجموعةً في ثلاث
عائلات: حروف الطباعة · خطّ الكرّاسة · خطّ اليد. والعيّنة صارت سيريلية
لا لاتينية — تختار خطًّا لنصٍّ روسي.

والتغطية **تُقاس على جهازك** بعرض النصّ المرسوم لا بـ
`document.fonts.check` (التي تُطابق سلسلة العائلات فتقول «نعم» عن خطٍّ
لم يصل). ونفرّق بين «الخطّ بلا سيريلية» و«الخطّ لم يصل»: الأولى وسمٌ
على الرقاقة، والثانية سطرٌ واحد أعلى اللوحة — لا نلوم خطًّا حين تكون
الشبكة هي المقطوعة.

**Dancing Script حُذف**: لاتينيٌّ فقط، فحلّ محلّه **Bad Script**.
والجلسات الحاملة `fontId: 'dancing'` تعود لخطٍّ صالح لا لـ`undefined`.

> ⚠️ القياس على الشبكة الحقيقية لم يتمّ **هنا**: بيئة التطوير تحجب
> `fonts.googleapis.com`، والتحقّق أظهر مسار «لم يصل» يعمل كما صُمِّم.
> الحكم على كل خطّ يقع على جهاز المستخدم عند أول فتحٍ باتصال.

**بند 13 — على مرحلتين، والأولى لم تكن كافية.**

المرحلة الأولى كانت **بطاقة المصدر**، ووُسمت مُنجَزة. وكان ذلك
سابقًا لأوانه: البطاقة **تسمّي** المصدر ولا **تعرضه**. وشكوى التدقيق
كانت أدقّ من ذلك — الصورة لها لوحةٌ كاملة (تمرير، تثبيت، تحجيم،
تكبير) بينما السكريبت والمحادثة لا شيء لهما إلا مقاطع ممارسة
مُقسَّمة. فالمرحلة الثانية **لوحة الأصل**: إطارٌ نصّيٌّ بنفس منطق
إطار الصورة — يُمرَّر داخله ويُحجَّم بمقبض ويُطوى، والطيّ يُحفظ.

والمحادثة تُعرَض فيه **كمحادثة**: أدوارٌ بأصحابها بترتيبها ودورك
بلونه. وفي المختارات: النصّ كاملًا وما تركتَه **يخفت ولا يختفي**،
ومعه عدده في العنوان. أن ترى ما لم تختره في سياقه هو الفرق بين
«اخترت خمسًا» و«اخترت خمسًا من ثمانيَ عشرة وأعرف ما تركت».

**بند 13 — بطاقة المصدر.** «المشهد والنصّ الأصلي» جملةٌ صادقة ولا
تفيد: لا تعرف منها أسكريبتٌ هذا أم دور متحدّث أم مختاراتك أم نصّ
OCR. صارت بطاقةً تسمّي المصدر وتعدّه **بوحدته** («جزء» في المحادثة
لا «جملة»)، ومعها زرّ «افتح الأصل» — الظلّ طبقةٌ فوق المحتوى لا
بديلٌ عنه. واسم المتحدّث صار على كل سطر في جلسات المحادثة، ودورك
مميَّز بلونه.

المنطق في الخدمة (`describeSource`) خالٍ من القاعدة، والشاشة تجلب
العنوان وتمرّره — فيبقى مُختبَرًا بلا تهيئة مستودعات.

**عطبٌ وُجد في الطريق:** `parseISODate` تنادي `.split` على مدخلها
مباشرةً، بينما ترويسة الوحدة نفسها تقول إن التخزين «ISO **أو** طابع
زمني رقمي». رقمٌ واحد في `scene.date` — من استيراد أو نسخةٍ مسترجَعة
— يُعطي `iso.split is not a function` **فتسقط شاشة الذكرى كلها**.
ظهر أثناء التحقّق البصري لا في اختبار. صارت الدالّة تقبل ما تَعِد به
الترويسة (نصّ ISO بوقتٍ أو بدونه · طابع رقمي · كائن `Date`)، ومعها
`toISODate` للتطبيع، و`monthKey` تمرّ به فلا تنشأ مجموعةٌ بعنوانٍ
فارغ في «حياتي». وله ملفّ اختبارٍ كامل (13) لأنه تحت كل عرض تاريخ في
التطبيق.

**ملفّات:** `services/shadow/playback-controller.js` ·
`services/shadow/fonts.js` · `services/shadow/shadow-session-service.js` ·
`views/shadow-view.js` · `utils/dates.js` · `css/shadow.css` ·
`tests/shadow.test.js` (+29) · `tests/dates.test.js` (جديد، 12)

### 0-و · النطق الأصلي *(بند 22)* — ✅ **مُنجَز**

`services/shadow/native-audio.js`: ثلاثة مصادر بالترتيب (Wikimedia
Commons · Lingua Libre · openrussian)، كلٌّ يفشل وحده فلا يُسقط ما
بعده.

**هذه الميزة الوحيدة في التطبيق التي تغادر الجهاز**، فقواعدها أشدّ:

- **مطفأة افتراضيًّا**، ولها اختبارٌ يعترض `fetch` ويطالب بألّا يُنادى
  قبل الموافقة. مُتحقَّق في متصفّح حقيقي بمراقبة **كل** طلبٍ يغادر
  الأصل: صفرٌ قبل الموافقة.
- نافذة الموافقة **تسمّي الخوادم** وتقول بالحرف إن الكلمة وحدها تخرج
  — لا جملتك ولا ذكراك ولا معرّف جهازك.
- ما يُجلب يُخزَّن ببايتاته في `nativeAudio` (v6)، فلا تخرج الكلمة
  مرّتين.
- **للكلمات المفردة فقط** — الجملة تبقى TTS والزرّ يقول ذلك ساعة
  تختاره.
- **السقوط مُعلَن لا صامت**: «مالقيناش تسجيل — نطقناها آليًا».
- **السحب يمسح** ما جُلب. موافقةٌ مسحوبة وبيانات باقية ليست سحبًا.
- لوحة **«ما يغادر جهازك»** في الإعدادات: متى وافقت، كم تسجيلًا، كم
  يزن، وزرّ يقفل ويمسح. الموافقة تُعطى في سياقها ولا تُدفَن هناك.

**التسمية.** `audioSource: 'human'` كان يعني *تسجيلك أنت* — اسمٌ
يحتمل الكذب بدخول الناطق الأصلي. صار ثلاثة: `tts` · `mine` ·
`native`، و`normalizeAudioSource` تقرأ القديم فتعيد الجديد. **لا
ترقية بيانات ولا لمس سجل** — مُتحقَّق على قاعدة v5 حقيقية: القرص ما
زال يحمل `'human'` ويُقرأ `'mine'`.

**وزرٌّ لا يعد بما لا يملك.** جلسةٌ بلا تسجيل مربوط كانت تعرض
«🎙 تسجيلي» وهي تنطق آليًّا. المحرّك كان يسقط إلى TTS صحيحًا، والكذبة
في التسمية وحدها — وهي أسوأ ما يكون لأنك تصدّقها. ظهرت في التحقّق
البصري لا في اختبار، فصار المصدر يُحصر فيما هو متاح
(`effectiveAudioSource`) وله ثلاثة اختبارات.

**«قالوا لا» غير «ما استطعنا السؤال».** `navigator.onLine` تكذب:
وسيطٌ يحجب أو بوّابة فندق كلاهما «متّصل». تخزين «غير موجودة» حينها
يُبقي الكلمة بلا نطقٍ للأبد بعد عودة الشبكة. فالسلبيّ لا يُخزَّن إلا
إذا **أجاب** خادمٌ واحد على الأقل؛ وإلا فالحال `unreachable` ولا
نتذكّر شيئًا. وهذا بالضبط ما ظهر في التحقّق هنا (البيئة تحجب
الخوادم): الرسالة «مقدرناش نوصل» لا «مالقيناش».

**الترقية v6** — إضافة محضة: store واحد بمفتاحه `word`. مُتحقَّق على
قاعدة v5 بُنيت بقائمة المستودعات **ناقصةً** الجديد: 34 → 35 مستودعًا،
والبيانات القائمة سليمة.

**ومستبعَد من النسخة الاحتياطية.** ليس تحسينًا للحجم بل خاصيّة
خصوصية: نسختك ملفٌّ تنقله وتشاركه، فلا يحمل بايتاتٍ ليست لك. وله
اختبارٌ يطالب بأن يكون **كل** مستودع إمّا في النسخة وإمّا مُستثنًى
بسببٍ مكتوب.

> ⚠️ **ما لم يُتحقَّق هنا:** بيئة التطوير تحجب
> `commons.wikimedia.org` و`api.openrussian.org` (المُخبِر: 403 على
> نفق الوسيط). فمسار **النجاح** — أن تعيد هذه الخوادم فعلًا ملفًّا
> بهذه الصيغ — لم يُختبَر على شبكة حقيقية. المُتحقَّق هنا: الموافقة،
> ومنع الخروج قبلها، والوجهات المقصودة، والسقوط المُعلَن، والذاكرة،
> والسحب. أوّل تشغيل على جهازك باتصالٍ حقيقي هو ما يحكم على الصيغ.

**ملفّات:** `services/shadow/native-audio.js` (جديد) ·
`playback-controller.js` · `views/shadow-view.js` ·
`views/settings-view.js` · `db/schema.js` · `db/migrations.js` ·
`db/repositories.js` · `utils/ids.js` · `services/backup/backup-format.js` ·
`services/trash-service.js` · `tests/shadow.test.js` (+24) ·
`tests/backup.test.js` (+4) · `tests/trash.test.js`

### 0-ز · الكتاب: يسرى دافئة، يمنى كونية *(بندان 80 و109)* — ✅ **مُنجَز**

- اليسرى: ورق دافئ فاتح، حبر داكن، ومِحبرةٌ بنّية بدل البنفسجي —
  «العالم الذي عشته».
- اليمنى: **سطح الصفحة نفسه** نيليٌّ عميق بنجومٍ خافتة وتدرّج عمقٍ من
  الكعب إلى الحافّة — لا بطاقة داكنة داخل صفحة.
- الكعب: ظلٌّ داخليّ على حافّتَي الصفحتين يصنع الطيّة. انحناء **مقيَّد**
  — كتابٌ مفتوح على منضدة لا صفحةٌ تُقلَب.
- الجملة الجارية **في** الفضاء لا على بطاقةٍ فوقه: الحدّ يخفت وتصير
  الخلفية هالةً تتلاشى، ويعود الحدّ عند النطق.
- إبراز السطر الجاري في صفحة المصدر: دفءٌ خفيف لا كاريوكي.

**التنفيذ بإعادة تعريف التوكِنات على `.sh-left` وحدها**، لا بإعادة
كتابة مكوّناتها: كل ما في الصفحة يقرأ `--sh-ink` و`--sh-line` أصلًا،
فقلبُ القيم يقلب الصفحة كلها — وأي مكوّن يُضاف غدًا يرث العالم الصحيح
بلا سطرٍ إضافي.

> **عرضٌ محض (بند 84).** حذف القسم من `css/shadow.css` يُرجع الشكل
> القديم ولا يغيّر سلوكًا واحدًا. ويحترم `prefers-reduced-motion` —
> ولا حركة فيه أصلًا.

**عطبٌ كشفته اللقطة:** شريط التحديد وصفّ الحفظ السريع كانا **ظاهرَين
في كل جلسة** بلا أن يطلبهما أحد. السبب أعمّ منهما: قاعدة المتصفّح
`[hidden] { display: none }` تُهزَم بأي `display` يكتبه المؤلّف، لأن
`.sh-select-bar` أخصّ من `[hidden]`. فكل مكوّن يُخفى بالسمة ويُعطى
`display: flex` يظهر دائمًا — وكانت تُرقَّع واحدةً واحدة
(`.sv-row[hidden]`, `.fab[hidden]`). صارت في `css/base.css` قاعدةً
واحدة `[hidden] { display: none !important; }` تُنهي الترقيع: مكوّنٌ
جديد يُخفى بالسمة يعمل من أوّل مرّة.

لم يكشفه اختبار — كشفته لقطةُ شاشة. وهذا سبب وجود خطوة التحقّق
البصري في قاعدة 6.

**ملفّات:** `css/shadow.css` · `css/base.css`

---

## 9.4 WS1 — الأساس الدلالي

### 1-أ · ترقية v7 — الأنواع تصير كيانات — ✅ **مُنجَز**

**المشكلة:** الأنواع كانت كتلة JSON في `settings['scene.types']`.
الكتلة تُقرأ كاملةً أو لا تُقرأ، وما يلزم استعلامٌ لا قراءة: «مَن يحمل
هذا الاسم المطبَّع؟» عند كل ضغطة مفتاح، و«كم ذكرى تستعمل هذا النوع؟»
قبل أي تغيير عام.

**وهي أوّل ترقية تنقل معنًى قائمًا** بدل أن تضيف بجانبه — كل ما قبلها
كان إضافةً محضة. الحيلة التي جعلتها آمنة: **البذر بالمعرّفات الحالية
نفسها**. `scene.type` يحمل `meeting` و`inspection` أصلًا، فبمجرّد
وجودها في `eventTypes` صار كل مشهدٍ موصولًا بنوعه — **بلا كتابة بايتٍ
واحد في `scenes`**.

التفصيل الكامل والجدول في `docs/03-architecture.md §3.6` كنموذجٍ
يُحتذى للترقيات القادمة. وثلاث شبكات أمان: الكتلة القديمة لا تُحذف،
و`scene.type` لا يُلمس، والبذر بـ`put` فإعادة التشغيل محايدة.

**ما صار ممكنًا بعدها:**

| البند | الحال |
|---|---|
| 9 — إعادة التسمية عامّة | ✅ تلقائيًّا (المعرّف هو الهويّة)، **ومُثبَتة** في متصفّح حقيقي: عدّلنا الاسم فتبدّل في «حياتي» وشاشة الذكرى، والذكرى لم تُكتَب |
| 10 — حوار التبعة | ✅ «مستعمَل في 2 ذكرى» ← [حدّث في الكل] · [اعمل نوعًا جديدًا] · [إلغاء] |
| 11 — كشف التشابه | ✅ تطبيع + مسافة تحرير بحدٍّ أعلى، **اقتراح لا دمج تلقائي** |
| النقل والترتيب | ✅ `moveType` (وتمنع الحلقات) · `reorderTypes` |
| الأسماء البديلة | ✅ `addAlias`/`removeAlias`، وتُحسب في كشف التعارض |

**تقسيم العمل بين الكاشفَين** — استُخرج من اختبارٍ سقط: `checkName`
تمنع المتطابق **بعد التطبيع** قبل الحفظ، فلا يمكن إنشاء زوجٍ متطابق من
الواجهة أصلًا. وما يبقى لـ`similarTypes` هو المتقارب لا المتطابق.
وحالة «نفس الاسم بالضبط» فيها تبقى مع ذلك: تنشأ من بياناتٍ دخلت من
طريقٍ آخر — الكتلة القديمة، أو استرجاع نسخة، أو استيراد — وهي أحقّ ما
يُعرَض.

**ملفّات:** `db/seeds.js` (جديد) · `db/schema.js` · `db/migrations.js` ·
`db/repositories.js` · `utils/ids.js` · `services/type-service.js`
(أُعيد بناؤه على المستودع بنفس الواجهة) · `services/scene-service.js` ·
`services/trash-service.js` · `components/type-manager.js` ·
`docs/03-architecture.md` · `tests/types.test.js` (+16)

### 1-ب · مكتبة الأشخاص *(بنود 41–44)* — ✅ **مُنجَز**

`people` كان موجودًا منذ أوّل يوم وفارغًا، و`conversationParts.personId`
موجودًا ودائمًا `null`. الفتحات محفورة وتنتظر.

**لماذا يستحقّ المتحدّث كيانًا؟** لأن `speaker` نصٌّ حرّ، فـ«أليكسي»
و«Алексей» و«م. أليكسي» ثلاثةُ أشخاص عند التطبيق وواحدٌ عندك. ومعنى
ذلك أن سؤالًا بديهيًّا — «فين كل الكلام اللي قاله أليكسي؟» — بلا جواب.

**والنمط هو نمط الأنواع بحرفه**: كيانٌ بمعرّف ثابت واسمٍ للعرض ومطبَّع
وأسماء بديلة وحالة، ولوحةٌ على قالب `type-manager`. بُني مرّةً ليُقرأ
ثلاثًا — والمشاريع والخيوط بعده.

**⚠️ القاعدة الحاكمة: لا دمج تلقائي.** المطابقة بالتطبيع **تقترح ولا
تقرّر**. شخصان باسمٍ متشابه قد يكونان اثنين فعلًا، والخطأ هنا يخلط
كلام رجلين — وهو خطأ لا يُكتشَف بعد شهر. فلا ترقيةَ بيانات تربط شيئًا:
`unlinkedSpeakers()` تعرض كل اسمٍ حرٍّ بعدد أجزائه ومشاهده ومعه
اقتراحٌ إن وُجد، والربط بضغطةٍ منك. وله اختبارٌ صريح يطالب بأن تبقى
النسبة `null` رغم وجود مطابقةٍ تامّة.

**والنصّ الحرّ يبقى** من وجهين: `speaker` لا يُمسح عند النسبة (بند
107) — هو ما كتبتَه أنت وقتها، و`personId` مَن نظنّه — والمنتقي يقبل
اسمًا حرًّا بلا إنشاء شخص. مَن لن يتكرّر لا يستحقّ بطاقةً في مكتبتك؛
إجبارك على إنشاء شخصٍ لكل صوتٍ عابر يحوّل المكتبة إلى مقبرة.

**وبلا ترقية schema:** `people` بفهارسه موجود، و`personId` مفهرس
أصلًا. الحقول الجديدة على السجل لا تحتاج ترقية.

**عطبٌ كشفه التحقّق البصري:** المنتقي كان يبدأ متناقضًا — يعرض خيارًا
وخانةُ الاسم تعرض عكسه — لأن `wireSpeakerSelect` لم تُزامن عند
التركيب. صار له اختبار.

**ملفّات:** `services/person-service.js` (جديد) ·
`components/speaker-select.js` (جديد) · `components/people-manager.js`
(جديد) · `components/modal.js` (خطّاف `onMount`) ·
`services/content-service.js` · `modals/content-modals.js` · `app.js` ·
`css/components.css` · `tests/people.test.js` (جديد، 29)

---

### 1-ج · الخيوط والعلاقات المُصنَّفة *(بنود 24–31, 65–68)* — ✅ **مُنجَز**

**القرار المفاهيمي أوّلًا:** «خيط الحدث» يحتمل قراءتين — قضيّةٌ ممتدّة
لها بداية ونهاية، أو موضوعٌ دائم يجمع ما يخصّه للأبد. **المُعتمَد:
القضيّة.** «شحنة أبريل اللي اتأخّرت» تُفتَح وتُغلَق؛ و«التعامل مع
الجمارك» موضوعٌ لا ينتهي ومكانه `topics`.

ولذلك للخيط `status` و`endDate`، وسؤاله الحاكم: **«إيه اللي لسه
مفتوح؟»**

| القاعدة | كيف نُفّذت |
|---|---|
| **العضويّة علاقةٌ لا حقل** (بند 27) | صفٌّ في `relationships` بنوع `thread:scene`. المشهد لا يحمل `threadId` — وله اختبارٌ يحذف الخيط ويطالب بأن يبقى المشهد سليمًا. وهذا سبب تنظيف `relationships` قبله في v8 |
| **العلاقات مُصنَّفة** (بند 28) | سبعة تصنيفات، لا «مرتبط» عامّة. والاتجاه محفوظ: نفس الصفّ يُقرأ «نتيجة لـ» من طرف و«أدّى لـ» من الآخر |
| **الاقتراحات بأدلّة معلَنة** (بند 31) | نفس الأشخاص · تقارب التواريخ · نفس المكان · نفس النوع. تُعرَض بسببها بالنصّ، ودليلٌ ضعيف وحده لا يكفي، **والاقتراح لا يربط شيئًا** |
| **الإنشاء بأثر رجعي** (بند 30) | `threadFromScenes` — تكتشف بعد شهرين أن أربعة مواقف كانت قصّةً واحدة |
| **أسماء بشريّة لا معرّفات** (بند 29) | «بتكمّل قصّة موجودة؟» ← اقتراحات بأسمائها · قائمة · قصّة جديدة · **أو لا شيء** |
| **صفحة الخيط** (بند 66) | القصّة بترتيبها الزمني وبالفجوات بينها، وملخّصٌ يعيد المعرّفات لا الأعداد |

**و`endDate` تتبع الحالة ولا تُكتب يدويًّا:** «خلص» تختم، و«اتفتح
تاني» **تمسح الختم**. قضيّةٌ مفتوحة تحمل تاريخ انتهاء تناقضٌ يظهر في
كل شاشة تعرضها.

**ثلاثة أشياء لا نَدَّعيها:**
- «المدّة» على الصفحة **بين أوّل حدثٍ وآخره** لا مدّة القضيّة. قضيّةٌ
  مفتوحة لم يقع فيها شيءٌ منذ شهر أطول مما نعرف.
- «مين فيها» مَن تكلّم في مشاهدها، لا مَن يخصّه الأمر.
- ⚠️ **تطوّر اللغة داخل الخيط (بند 67) ما زال مؤجَّلًا** إلى ما بعد
  WS6: ادّعاء «استُخدمت بطلاقة» يحتاج دليلًا لا نملكه. البند 74 صريح.

**ترقية v9** إضافة محضة: store واحد، **ولا حقلَ يُضاف إلى `scenes`**.

**عطبٌ أمسكه فحص الإقلاع:** المسارات سُجِّلت والاستيراد لم يُضَف، فسقط
التطبيع عند الإقلاع بـ`renderThreads is not defined` — والاختبارات
الـ328 كلها خضراء. هذا بالضبط ما أُضيف الفحص لأجله.

**ملفّات:** `services/thread-service.js` (جديد) ·
`views/threads-view.js` (جديد) · `modals/thread-modals.js` (جديد) ·
`db/schema.js` · `db/migrations.js` · `db/repositories.js` ·
`utils/ids.js` · `components/icons.js` · `views/scene-view.js` ·
`app.js` · `css/components.css` · `tests/threads.test.js` (جديد، 25)

---

### 1-د · المشروع — ⏸️ **قرارٌ مؤجَّل عمدًا** *(بند 25)*

**ليس نسيانًا ولا نقصًا في التنفيذ — قرارُ منتَجٍ مُتَّخَذ.**

القاعدة الحاكمة في هذا المشروع: **كل كيان لازم يجاوب سؤالًا حقيقيًّا.**

| الكيان | سؤاله |
|---|---|
| نوع الحدث | «الذكرى دي من أي نوع؟» |
| الشخص | «فين كل الكلام اللي قاله أليكسي؟» |
| الخيط | «إيه اللي لسه مفتوح؟» |
| **المشروع** | **لم يتّضح بعد** |

فالخيوط تغطّي «الشغل اللي بشتغل عليه» تغطيةً واسعة، والفرق المزعوم أن
المشروع أوسع وأطول — وهو فرقٌ **لم يُختبَر في استعمالٍ حقيقي بعد**.
وكيانٌ يُضاف بلا سؤالٍ يجيبه يصير حقلًا تملؤه ولا تقرؤه.

**القرار:** يُؤجَّل حتى تُستعمل الخيوط فعليًّا، فإن ظهرت الحاجة ظهر
معها **شكلها المضبوط**. راجع `docs/06-roadmap.md`.

#### ما فُعل ليبقى التأجيل بلا كلفة

التأجيل الرخيص هو الذي لا يفرض هدمًا لاحقًا. وثلاثة أشياء تضمن ذلك:

1. **العضويّة اصطلاحٌ عامّ لا حالةٌ خاصّة.** `membershipKind(container,
   member)` يُنتج `"thread:scene"` اليوم و`"project:thread"` غدًا. لا
   جدول جديد، ولا خدمة تُعدَّل، ولا ترقية.
2. **`containersOf(memberId, kind?)` يقرأ أي حاوٍ من أي نوع** — بما لم
   يُبنَ بعد. و`containersOfThread` موجودة وتعود فارغةً اليوم:
   **الخيط ليس أعلى مستوًى نهائيًّا**، وهذا مُختبَر لا موعود.
3. **لا حقول تخمينيّة.** كانت الخطّة الأولى تضع على كل خيط
   `projectIds[]` و`journeyIds[]` و`personIds[]` و`placeIds[]` —
   مصفوفاتٍ فارغة تنتظر ميزاتٍ لم تُبنَ. **أُزيلت**: حقلٌ فارغ على كل
   سجل هو حقلٌ ستُجبَر على ملئه أو تراه فارغًا للأبد، وهو نقيض
   «العضويّة علاقةٌ لا حقل». والأشخاص والأماكن تُشتقّ من مشاهد الخيط
   لا تُملأ يدويًّا.

**والحارس ستّة اختبارات** تُنشئ روابط `project:*` بالاصطلاح وحده — بلا
مستودع ولا خدمة ولا حقل — وتطالب بأن يقرأها العامّ وألّا تتسرّب إلى
الخاصّ (`threadsOfScene` لا يرى المشروع كخيط).

**أثره على ما لم يُبنَ بعد:**

| المتأثِّر | لماذا لن يحتاج إعادة بناء |
|---|---|
| **Event Threads** | لا تفترض أنها القمّة؛ `containersOfThread` جاهزة |
| **Living Memory Atlas** | يقرأ `containersOf` العامّ، فيرى المشروع يوم يوجد بلا تعديل |
| **Prepared Scene Import** | يكتب عضويّةً بالاصطلاح، فيستهدف أي حاوٍ |

---

### 1-هـ · العلاقات: `kind` هو الحقل *(«د-5»)* — ✅ **مُنجَز**

وُلد `relationships` بحقل `type`، ثم كتب الكود `kind` **وقرأه**، وبقي
`type` يُكتب معه بلا أن يُقرأ أبدًا. حقلان لنفس المعنى.

و`kind` هو الذي بقي لأنه اصطلاح المشروع كلّه — `media.kind` و
`savedItems.kind` و`contentBlocks.kind` — بينما `type` اسمٌ نتخلّص منه
في `scenes` أيضًا لصالح `eventTypeId`.

**ولماذا الآن؟** الخيوط (1-ج) تُبنى فوق هذا الجدول مباشرةً. تنظيفه
اليوم تنظيفُ جدولٍ صغير، وتأجيله تنظيفُه وتحته خيوط.

**ترقية v8:** فهارس `kind` و`from_kind` و`to_kind` — تحوّل «هات روابط
هذا العنصر من هذا النوع» من مسحٍ في الذاكرة إلى إصابةِ فهرس — وردمُ
`kind` من `type` للسجلات التي سبقت v4. و`type` **لا يُحذف من
السجلات**: حذف حقلٍ من بيانات قائمة ترقيةٌ إتلافية. وفهارسه تبقى
معلَنة لأن حذف فهرسٍ منشور ممنوع (§3.6) ورجوعٌ لكودٍ أقدم يستعملها.

**وعطبٌ حيٌّ وُجد في الطريق — وهو الأهمّ:** `unlink` كانت تنادي
`relationships.remove`، **ودالّةٌ بهذا الاسم غير موجودة في المستودع
أصلًا** (الاسم الصحيح `destroy`). فكل إلغاء ربطٍ يرمي، ويُسقط معه
`Promise.all` في نافذة الربط — **فلا يُحفظ حتى ما أضفتَه في نفس
الجلسة**. أي أن إلغاء تحديد أي رابط كان يُبطل الحفظ كلّه صامتًا.

عاش العطب لأن `link-service.js` **لم يكن له اختبارٌ واحد**. فحص رسم
الوحدات يستورد الملفّ ولا ينادي دوالّه — و**استيراد ملفّ ليس اختبارًا
له**. صار له الآن 15 اختبارًا، وأُثبت أنها تمسك العطب: أُعيد الاسم
الخاطئ عمدًا فسقطت ثلاثة منها.

**ملفّات:** `db/schema.js` · `db/migrations.js` ·
`services/link-service.js` · `tests/links.test.js` (جديد، 16)

---

## 9.5 WS2 — استيراد المشهد المُجهَّز *(بنود 32–37)* — ✅ **مُنجَز**

**ولماذا هذا أوّلًا بعد WS1، لا الأطلس؟** لأن الاستيراد **يكتب** في
كيانات WS1 — الأشخاص والأنواع والخيوط والتعبيرات — فيمتحنها امتحانًا
حقيقيًّا قبل أن يُبنى فوقها شيء. والأطلس **يقرؤها**؛ وأطلسٌ يُصمَّم على
قاعدةٍ فارغة يتّخذ قرارات عرضٍ خاطئة لأنه لم يرَ بياناتٍ كثيرة قطّ.
فالاستيراد يملأ القاعدة، والأطلس يجدها مملوءة.

ثلاث طبقاتٍ منفصلة عمدًا، لأن كلًّا منها تجيب سؤالًا مختلفًا:

| الطبقة | السؤال | تلمس القاعدة؟ |
|---|---|---|
| `import/parse.js` | هل الحزمة سليمة الشكل؟ | ✗ |
| `import/plan.js` | ما علاقة ما فيها بما عندك؟ | قراءةً فقط |
| `import/apply.js` | اكتُبها | ✓ |

وفصلُ الأولى مقصود: التحقّق من الشكل يعمل على أي جهاز بلا قاعدةٍ
مفتوحة، فيمكن اختباره حتميًّا، ويمكن أن يقول إن الملفّ تالف **قبل** أن
نلمس بياناتك بشيء.

### 2-أ · صيغة الحزمة والتحقّق الصارم

**كريمون فيما نقبل، صارمون فيما نلتزم به.** المُنتِج الخارجي يرسل مرّةً
`title` ومرّةً `titleAr`، ومرّةً تاريخًا نصًّا ومرّةً طابعًا رقميًّا.
فالقراءة تتسامح مع الأشكال المعروفة، والكتابة لا تتسامح: ما لا نفهمه
يُرفَض بسببٍ مكتوب ولا يُخمَّن. وصيغةٌ من المستقبل تُرفَض ولا تُقرَأ
بقواعد اليوم — التفسير الخاطئ الصامت أخطر من الرفض الصريح.

**وما لا يستوعبه التطبيق يُعلَن ولا يُبتلَع.** المواصفة تعدّ أربعةً
وعشرين نوعًا؛ نمثّل سبعةً تمثيلًا حقيقيًّا — لكلٍّ خدمةٌ تكتبه وشاشةٌ
تعرضه — والباقي مُعلَنٌ بسببٍ مكتوب لكلّ واحد. استيراد نوعٍ بلا شاشة
كتابةٌ في العدم: تظنّه وصل وهو ليس. واختبارٌ بنيويّ يمنع أن يدخل نوعٌ
للمواصفة بلا قرارٍ فيه، وآخر يمنع أن يدخل بلا اسمٍ عربيّ.

### 2-ب · الخطّة وحلّ التكرار

**نقترح ولا ندمج.** «أحمد صلاح» في الحزمة و«أحمد صالح» عندك: الآلة ترى
حرفًا فرقًا، وأنت ترى شخصين أو واحدًا. فالمتقارب يُنشَأ جديدًا افتراضًا
ويُعرَض عليك البديل، لا العكس — الدمج الخاطئ يفسد سجلّ شخصين معًا،
وفصلُ ما كان واحدًا تصلحه بضغطة.

**والمتطابق ليس دمجًا بل هويّة:** نفس الاسم المطبَّع = نفس الكيان،
والأسماء البديلة تُحسب. لا نسأل عمّا لا سؤال فيه.

**ولكل قرارٍ دليلٌ مكتوب:** «نفس الاسم بالضبط»، «اسمٌ تاني عندك»،
«الفرق حرف واحد»، «الاسم ده جزءٌ من اسمٍ عندك».

ومسافة التحرير انتقلت إلى `utils/normalization.js`: يستعملها كشف
الأنواع المكرّرة (بند 11) ومطابقة أسماء الحزمة معًا، فلا تستورد إحداهما
من الأخرى لتأخذ حرفًا مشتركًا.

### 2-ج · التنفيذ

**عبر الخدمات لا فوقها.** كل كتابةٍ تمرّ بـ`createScene` و`addPerson`
و`addScript` و`addExpression`، لأن الخدمات تحمل ما لا تراه: الذكرى تأتي
بكتلة نصّها الأصلي، والسكريبت بنسخته الأولى في التاريخ، والتعبير كيانٌ
عالميّ يُسجَّل له ظهور. استيرادٌ يكتب في المستودع مباشرةً يُنتج صفوفًا
**ناقصةً بصمت** — تبدو سليمة حتى تفتحها.

**وإمّا كلّه وإمّا لا شيء.** لا معاملةَ واحدة تلفّ عشرات الكتابات عبر
الخدمات، فيُمسَك **دفترٌ** بكل ما كُتب ويُتراجَع عنه بالعكس عند أي فشل.
والدفتر يسجّل ما أنشأناه نحن وحده: شخصٌ كان موجودًا لا يُمسّ، ومحادثةٌ
سابقة في ذكرى مُلحَق بها لا تُمحى، ومعنى تعبيرٍ ملأناه يعود فارغًا كما
كان. وذكرى بنصف محادثةٍ وبلا تعبيرات **أسوأ من لا شيء**: تظنّها كاملة
فلا تُعيد الاستيراد، ويضيع نصفها بلا أن تعرف.

**والتقرير يُبنى من الدفتر** — ممّا كُتب فعلًا لا ممّا نُوِيَ.

### 2-د · المعاينة قبل الالتزام *(بند 36)*

`/import`: الصق ← عايِن ← استورد. ولا رقم بلا مصدر — كل عدد يُحسَب من
القرارات لحظة عرضه، فاستبعادُ تعبيرٍ ينزل بالرقم فورًا (بند 89).

### ما وجدته المعاينة في نفسها عند تجربتها في متصفّح حقيقي

لم يكشفه اختبار: «11 11 حاجات» (الرقم يُطبَع مرّتين، والأحد عشر يعود
للمفرد) · «مستبعَد باختيارك» وبعضه لم تختره أنت · أسماء ما لا يُستورَد
بمعرّفاتها الإنجليزيّة في شاشةٍ عربيّة · شريط الالتزام شبه شفّاف فيظهر
الكلام من خلفه · وتقريرٌ يعرض `scriptVersion` و`contentBlock` — وهي
أجزاءٌ ممّا استوردتَه لا أشياءُ استوردتَها، فعرضُها عدٌّ مزدوج.

### وعطبٌ قديم وُجد في الطريق — تهريبٌ مرّتين

الوسم `html` يهرّب كل قيمةٍ مُدرَجة وحده، فـ`${esc(x)}` بداخله تهريبٌ
مرّتين: ذكرى اسمها «قعدة "الشلّة"» تُعرَض والاقتباس فيها `&quot;`
حرفيًّا. **ستّةٌ وثلاثون موضعًا** في ستّة ملفّات — الخيوط والظلّ والذكرى
ونوافذ الخيط ومنتقي المتحدّث.

ولم يكشفه أيٌّ من الاختبارات الأربعمئة: نصوصها كلها بلا محارف خاصّة.
ظهر أوّل ما عُرضت رسالة محلّل JSON على الشاشة — وفيها علامات اقتباس
دائمًا.

ولأن اختبارًا لم يكشفه، **الحارس ليس اختبارًا** بل فحصٌ ساكن في
`run-headless.mjs` يمشي على كل وحدة في `js/` كفحص رسم الوحدات: يقرأ
القوالب، ويعرف الموسوم من العادي (فـ`esc` داخل قالبٍ عادي صحيحةٌ
ولازمة، وواحدٌ وثلاثون موضعًا منها بقي)، ويسقط عند أوّل تهريبٍ مزدوج
جديد.

**ملفّات:** `services/import/package-format.js` · `parse.js` · `plan.js`
· `apply.js` (كلّها جديدة) · `views/import-view.js` (جديد) ·
`utils/normalization.js` · `services/type-service.js` ·
`components/icons.js` · `views/settings-view.js` · `app.js` ·
`css/components.css` · `tests/import.test.js` ·
`tests/import-plan.test.js` · `tests/import-apply.test.js` (جديدة، ٩٢
اختبارًا).

---

## 9.6 WS4 — الأطلس الحيّ *(بنود 45–64)* — ✅ **مُنجَز**

**ولماذا بعد الاستيراد؟** لأن الأطلس يقرأ الكيانات، والاستيراد يكتبها.
وأطلسٌ يُصمَّم على قاعدةٍ فارغة يتّخذ قرارات عرضٍ خاطئة لأنه لم يرَ
بياناتٍ كثيرة قطّ. فمُلئت القاعدة أوّلًا **عبر مسار الاستيراد الحقيقي**
— اثنتا عشرة ذكرى في سبعة شهور بفجواتٍ حقيقيّة — ثم صُمِّم الأطلس على
صفوفٍ كتبتها الخدمات لا على كائناتٍ مصنوعة في الشاشة.

**والمواصفة الكاملة في `docs/11-atlas.md`.** هنا ما يخصّ الخطّة:

### الترتيب المنفَّذ ولماذا

النهر ← اليوم ← القصص المكمّلة ← الأشجار ← المحور ← العدسات ← الكوكبة.

**التصفّح العملي قبل الخيال البصري**، والكوكبة آخرًا لأنها أقلّها
عملًا. والعرض الأوّل بلا رسمٍ متحرّك بقصد: أوّلًا يصحّ التنقّل والربط —
تضغط يومًا فتصل إليه وتضغط شخصًا فيُصفّى النهر عليه — ثم يأتي الخيال
البصري فوق أساسٍ ثابت. الترتيب المعكوس يُنتج شيئًا جميلًا لا يُتنقَّل
فيه.

### مراجعة الاعتمادات قبل البناء — وما كشفته

فُحص كل محورٍ قبل كتابة سطر، فتبيّن أن **نصفها غير موجود فعلًا**:

| المحور | الحال الحقيقيّ |
|---|---|
| الزمن · النوع · الخيط | كامل |
| الشخص | **مُشتقٌّ** من `conversationParts.personId` |
| المكان | نصٌّ لا كيان — `placeId` يبقى `null` دائمًا |
| الموضوع · الرحلة · المشروع | **غائب** — مستودعاتٌ بلا خدمة |

و`scene.peopleIds` و`topicIds` و`journeyId` و`placeId` تُكتب فارغةً عند
الإنشاء **ولا تُملأ في أي مكان** — حقولٌ ميّتة من الجيل الأوّل. فلا
تُقرَأ، والاشتقاق من مصادرها الحقيقيّة — وهو نفس مبدأ §3.6.1.

### لا عالمَ ثانيًا

كل شاشات الأطلس تقرأ من `atlas-service` وحده، وهو يشتقّ المحاور **عند
القراءة** من `scenes` والعلاقات وأجزاء المحادثة كما هي. لا مستودعَ
للأطلس ولا حقلَ يُملأ له — **وWS4 كلّه أُضيف بلا ترقية قاعدة واحدة**.

### عطبان وُجدا في الطريق

**١. الصفحات تُسقط ذكرياتٍ بصمت.** `repo.page()` تقيس حدّها على ما
**بعد** الترشيح، فدفعةٌ ممتلئة قد تنقطع في منتصف يوم — ولو حُسب ذلك
اليوم كاملًا وتُجووِز لاختفى نصفه. وهو نفس السقف الصامت الذي أُزيل من
«حياتي» في 0-ج. اختبارٌ بخمسٍ وسبعين ذكرى في يومٍ واحد يحرسه.

**٢. `${cond ? html`…`}` بلا `raw`** — من عائلة التهريب المزدوج: القالب
الداخلي يُنتج نصًّا فيهرّبه الخارجي، فتُطبَع الوسوم حرفيًّا. امتدّ الفحص
الساكن ليشمله. وأوّل نسخةٍ منه فحصت أوّل حروف الإدراج وحدها فمرّت على
البُنية الشائعة — أُعيد العطب تجريبًا فلم يسقط الفحص، فصار على المدى
كلّه.

**وعطبٌ ثالث في الاستيراد كشفه الأطلس:** حزمةٌ تُلحَق بخيطٍ موجود وتحمل
حالةً مخالفة كانت تُتجاهَل صامتةً. لا تغييرَ لها — إقفال قضيّةٍ لأن
ملفًّا قال ذلك خارج ما وافقتَ عليه — لكنها صارت تُقال في التقرير.

**ملفّات:** `services/atlas-service.js` · `views/river-view.js` ·
`views/facets-view.js` · `views/constellation-view.js` (كلّها جديدة) ·
`utils/plural.js` (جديد) · `utils/dates.js` · `views/scene-view.js` ·
`app.js` · `index.html` · `css/components.css` · `tests/atlas.test.js`
(جديد، ٥٦ اختبارًا).

---

## 9.7 WS3 — الأشخاص والأصوات والصور — ✅ **مُنجَز**

**ولماذا بعد الأطلس؟** لأن «مين كان هنا» يحتاج اشتقاق الأشخاص الذي
بناه الأطلس. بناؤه قبله كان سيعني كتابته مرّتين — أو قراءة
`scene.peopleIds` الميّت وعرض «مفيش حد» في ذكرى فيها ثلاثة تكلّموا.

**ثلاثة حقولٍ ميّتة كُشفت في هذه الجولة كلها**، تُكتب فارغةً عند
الإنشاء ولا تُملأ في أي مكان: `peopleIds` (كشفه الأطلس)، و`caption`
(كشفه عارض الصورة)، ومعهما `topicIds` و`journeyId` و`placeId`. اثنان
منها صارا حيّين — والوصف الآن يُكتب من العارض — والباقي مُعلَنٌ في
`ABSENT_AXES`.

**وعطبٌ حيٌّ كشفه بناء طابور الأصوات: مالكان لرابطٍ واحد.** خدمة
الصوت كانت تُحرّر رابط المقطع السابق بينما كاش `media-service` لا يزال
يحتفظ به. المسار: شغّل تسجيلًا، ثم آخر، ثم عُد للأوّل → «الملف مش
موجود» وهو موجود. أُثبت في متصفّح **قبل** الإصلاح بـ`fetch` على
الرابط المُعاد من الكاش.

فالملكيّة صارت في `media-service` وحده، والمُشغّل يُعلن ما يستعمله
(`pinMedia`) ولا يُحرّر. و`releaseUrls` تتخطّى المثبَّت: الصوت يكمل بعد
مغادرة الشاشة عمدًا، وتحريرُ رابطه يقطعه عند أوّل تقديم.

**ملفّات:** `services/audio-service.js` · `services/media-service.js` ·
`components/lightbox.js` · `components/mini-player.js` ·
`views/scene-view.js` · `app.js` · `css/components.css` ·
`tests/lightbox.test.js` (جديد، ١٣) · `tests/audio.test.js` (+١٢).

---

## 9.8 WS5-أ — حياة التعبير *(بنود 69–74)* — ✅ **مُنجَز**

**العطب:** في صفحة الذكرى شريطٌ من ستّ مراحل ومؤشّرٌ يقول «دلوقتي»
تحت واحدة. وكان يقول «سمعته» **دائمًا ولكل تعبير إلى الأبد**:
`masteryState` يُكتب `'heard'` عند الإنشاء ولا يرفعه شيءٌ في التطبيق
كلّه. شريطُ تقدّمٍ لا يتقدّم أسوأ من غيابه — يَعِد بقياسٍ لا يقع.

ولا يجوز أن يرفعه التطبيق وحده *(بند 18)*: الممارسة ليست إتقانًا،
وخمسون تكرارًا في الظلّ لا تعني أنك تستعملها مع بشر.

**فشيئان لا يختلطان:**

| | المصدر | مَن يكتبه |
|---|---|---|
| الوقائع | `expressionOccurrences` | التطبيق، من ظهورٍ حقيقيّ |
| المرحلة | `masteryState` | **أنت وحدك**، بضغطةٍ صريحة |

ومكتوبٌ تحت الأزرار في الشاشة إنها قولك لا حساب التطبيق.

### الكلمة مُشتقّة لا مخزَّنة

مستودع `words` فيه `lemma` و`pos` — **ولا يكتب فيه شيء**. والكلمات
تُلتقَط اليوم في `savedItems` بنوع `word` مع تصنيفٍ يقول لماذا
التقطتَها. وجعلُ `words` بيتًا ثانيًا لنفس الشيء هو «العالم الثاني»
الذي تتجنّبه بقيّة الخدمات.

فحياة الكلمة تُشتقّ عند القراءة: التقاطاتك، وأين يظهر نصّها في
المحادثات والسكريبتات والتعبيرات.

**وحدُّها مُعلَنٌ في `UNBUILT`** — بنفس مبدأ `ABSENT_AXES` و
`NOT_SUPPORTED`، ومعه اختبارٌ يرفض أي مدخلٍ بلا سبب: المطابقة **نصّيّة
لا صرفيّة**، «идти» لا تجد «шёл»، ويحتاج ذلك محلّلًا صرفيًّا روسيًّا.

### ثلاثة لم تظهر إلا في متصفّح

- **قسمان ينسخ أحدهما الآخر.** أوّل بديلٍ كتبتُه لشريط الإتقان سرد
  التعبيرات كلها — فصار «رحلة التعبير» نسخةً ثانية من «اللغة جوّه
  الذكرى» فوقه مباشرةً. فصار لا يعرض إلا ما ظهر **خارج** هذه الذكرى،
  ومعه عناوين ذكرياته، ويختفي كلّه إن لم يوجد.
- **شارةٌ تخالف قائمتها.** «التعبيرات ٩» وتحتها ثمانية — سقفٌ صامت
  قدرُه ثمانية. فسقفٌ يسع الاستعمال العاديّ، وسطرٌ صريح إن جاوزه شيء.
- **«سمعته» على كل سطر** تزحم النصّ الروسيّ حتى يلتفّ، ولا تقول شيئًا
  لأنها حال كل تعبيرٍ لم تحكم عليه. فالمرحلة تظهر حين تكون خبرًا.

**ملفّات:** `services/language-service.js` (جديد) ·
`views/language-view.js` · `views/scene-view.js` · `app.js`
(مسارَا `/expression/:id` و`/word/:text`) · `css/components.css` ·
`css/journal.css` (حُذفت قواعد `.life-track` الميّتة) ·
`tests/language.test.js` (جديد، ٢٩).

---

## 9.9 WS5-ب — منشأ الظهور *(بند 38)* — ✅ **مُنجَز، وسؤالٌ مفتوح**

**العطب:** `expressionOccurrences` مُعلَّقٌ في المخطَّط بأنه «كل ظهور
بتاريخه **ومصدره**». وكان `sourceType` يُكتب `'manual'` و`sourceQuote`
يُكتب `''` — **ثابتَين لكل ظهور** — بينما للنداءات الثلاثة منشأٌ مختلفٌ
فعلًا. حقلٌ يُكتب ثابتًا حقلٌ ميّت، وهو **سادس** ما وُجد من هذا الصنف
في هذه الجولة.

| المسار | المنشأ | ما يحمله |
|---|---|---|
| نافذة «تعبير جديد» | `manual` | لا شيء — أنت كتبتَه |
| `import/apply.js` | `import` | جملة `example` من الحزمة |
| النصّ الخارجي في الظلّ | `shadow` | معرّف الجلسة |
| ما لم يُمرَّر منشؤه | `unknown` | — |

**ولا يُخمَّن منشأ.** نداءٌ بلا `source` لا يُنسَب إلى «يدوي» كذبًا بل
إلى `unknown`، ويُقال في الشاشة «مش معروف منين».

**والحزمة صارت تحمل `example`** — الجملة التي ورد فيها التعبير تصير
اقتباس ظهوره. إضافةٌ اختياريّة: حزمةٌ قديمة بلا الحقل تمرّ كما هي،
واختبارٌ يحرس ذلك.

### الظهورات المكتوبة قبل هذا التغيير — ترقية v10 وزرُّ إقرار

كلها كانت تحمل `'manual'` **حرفيًّا** للمسارات الثلاثة، فظهورٌ قديم جاء
من استيرادٍ أو من الظلّ يقول «كتبته بإيدك» وهو لا يعرف. **وسُئل صاحب
البيانات فاختار الجمع بين الحلَّين:**

- **ترقية v10** تحوّل كل `'manual'` قديم إلى `'unknown'`. وهي ليست
  إتلافية رغم أنها تكتب فوق قيمة: الحقل كان ثابتًا لا يحمل خبرًا، فلا
  معلومةَ فيه تضيع. تستبدل ادّعاءً بلا سند بإقرارٍ صادق، لا أكثر.
  ولا تلمس `import` ولا `shadow` — واختبارٌ يمنع أن تصير عمياء.

- **وزرٌّ في الإعدادات** يقول «دول كلهم كتبتهم بإيدي»، يظهر فقط حين
  يكون فيه ما يُقَرّ به. فالتطبيق لا يدّعي، وأنت تقرّ بما تعرفه — وهو
  نفس مبدأ `setStage`: مَن يعرف هو أنت.

**ما لا يستعيده شيء:** أي ظهورٍ قديم جاء فعلًا من استيرادٍ أو من الظلّ
لن يُعرَف. الحقل لم يكن يحمل الفرق أصلًا، فلا مكانَ نقرأ منه الحقيقة.
الزرّ يُثبت ما تقوله أنت، لا ما كان.

**جُرِّبت على بياناتٍ حقيقية في متصفّح:** قاعدة v9 فيها ٢٢ ظهورًا
ارتقت إلى v10، فصار ١٩ منها `unknown` وبقي `import` و`shadow` كما هما،
ثم أخفت الضغطةُ اللوحةَ وحوّلت التسعة عشر.

**ملفّات:** `services/content-service.js` · `services/language-service.js` ·
`services/import/parse.js` · `services/import/package-format.js` ·
`services/import/apply.js` · `modals/content-modals.js` ·
`views/shadow-view.js` · `views/language-view.js` ·
`tests/language.test.js` (+٧) · `tests/import-apply.test.js` (+٣).

---

## 9.9-ب WS6-أ — التحليل المُشتقّ *(بنود 86–93)* — ✅ **مُنجَز**

**قرارٌ سُئل عنه أوّلًا.** خارطة الطريق تعلّق WS6 على «أي AI خارجي
للتحليل — وهو الذي يحدّد صيغة الحزمة». فسُئل صاحب البيانات، فاختار
الجمع بين مسارين: **تحليلٌ يُشتقّ من بياناته بلا أي نموذج**، ومعه
**حزمةُ تحليلٍ تُصدَّر وتُحلَّل خارجًا وتعود**. وهذا القسم هو الأوّل.

**والبناء عليه آمن الآن** لأنه لا يفترض شيئًا عن المسار الثاني: كل ما
فيه يُحسَب من صفوفٍ قائمة.

| القسم | من أين | والحدّ |
|---|---|---|
| أنماط التصحيحات | `mistakeComparisons` بنوعها | **لا نسبة** — المقام مجهول |
| ما تلتقطه ولماذا | `savedItems` بتصنيفاتها | **ليس حجم مفرداتك** — بل عكسه |
| أين لغتك تعيش | `facetTree` **نفسها** | لا عدّ ثانٍ يفترق يومًا |
| إيقاعك | تواريخ الذكريات | يقول متى كتبتَ لا متى عشتَ |
| ما تمرّنت عليه | `practiceEvidence` | تقرأ `impliesMastery: false` وتُعيدها |

### ولا يُختلَق تقدّم *(بند 74)*

أسهل شيءٍ في شاشة تحليل أن تقول «لغتك بتتحسّن ٪١٢». وهو ادّعاءٌ لا سند
له: التطبيق يرى ما سجّلتَه لا ما قلتَه.

فما لا يُقاس **مُعلَنٌ** في `NOT_MEASURED` — الطلاقة، والتقدّم، وحجم
المفردات، ونسبة الأخطاء — ومعه اختبارٌ يرفض مدخلًا بلا سببٍ مكتوب،
وآخر يطالب بألّا تعود النظرة العامّة برقمٍ مركَّبٍ واحد (`score` أو
`level` أو `grade`).

**وبند 67 يبقى مؤجَّلًا.** شرطُ إعادة النظر فيه كان «بعد WS6 حين يوجد
دليل» — ودليلُ الممارسة دليلُ ممارسةٍ لا طلاقة. فلا شيء تغيّر.

### وغلطةُ جمعٍ رابعة — والملفّ خرج بلا اختبار

`utils/plural.js` استُخرج بعد ثلاث غلطات، ثم أخطأ رابعةً: «وفيه **1
تصحيح** تاني». `counted` كانت تُسقط الرقم عن المثنّى ولا تُسقطه عن
المفرد. صار الواحد والاثنان بلا رقم — العربيّة تحمل العدد في الصيغة —
ومعهما `tests/plural.test.js` أخيرًا.

**ملفّات:** `services/analysis-service.js` (جديد) ·
`views/analysis-view.js` (جديد) · `utils/plural.js` · `app.js` ·
`index.html` · `css/components.css` · `tests/analysis.test.js` (جديد،
٢٥) · `tests/plural.test.js` (جديد، ٩).

---

## 9.9-ج WS6-ب — حزمة التحليل — ✅ **مُنجَز**

الشقّ الثاني ممّا اختاره صاحب البيانات: تحليلٌ بأي نموذجٍ يريده،
**بلا مفتاح ولا اتصالٍ ولا بايتٍ يخرج إلا بيده**.

```
ذكرى ── «حلّلها» ──▶ ملفُّ طلب  ──▶  أي محلِّل تختاره
                                          │
  ذكرياتك ◀── استورد ◀── معاينة ◀── ردٌّ ──┘
```

### الردّ حزمةُ استيرادٍ عاديّة — وهذا هو التصميم

الردّ يعود بصيغة **حزمة المشهد المُجهَّز نفسها** التي تعرفها
`import/parse.js` منذ WS2. ليس اختصارًا: ذلك المسار مبنيٌّ ومُختبَرٌ
ويمرّ بثلاث طبقات — تحقّقٌ صارم، ثم خطّةٌ لا تكتب شيئًا، ثم تنفيذٌ إمّا
كلّه وإمّا لا شيء — **ومعاينةٌ تريك كل صفٍّ قبل الالتزام**. صيغةٌ ثانية
تعني ثلاث طبقاتٍ ثانية ومعاينةً ثانية، وبابًا لبياناتٍ تدخل بلا مراجعة.

### التعليمات داخل الملفّ

تفتح الملفّ، تنسخه، تلصقه لأي محلِّل — فيقرأ معه ما المطلوب وبأي صيغةٍ
يردّ. تعليماتٌ في وثيقةٍ منفصلة تُنسى. وفيها صراحةً: لا تخترع وقائع،
ولا تدّعِ إتقانًا، واكتب بالعامّية المصرية.

### وما يخرج معلومٌ بعدده قبل خروجه

الشاشة تقول قبل الحفظ: «هيتحفظ ملف فيه *أول زيارة للمكتب*: جزأين من
المحادثة، تعبيرين، ٣ تصحيحات. وأسماء: إيجور، أنا». عددٌ لا وصفٌ عامّ —
«موافق» على المجهول ليست موافقة.

### وعطبٌ في التصميم كشفه أوّل اختبارٍ للرحلة كاملة

الردّ كان يُنشئ **ذكرى ثانية** بدل أن يُكتب في التي حُلِّلت: الخطّة
تفترض الإنشاء دائمًا، وذلك صحيحٌ لأن مطابقة العنوان تخمين. فصار الطلب
يحمل `forSceneId` ويطلب من المحلِّل إعادته — **هويّةٌ لا مطابقة**،
فلا تخمينَ يُخشى. ويبقى ظاهرًا في المعاينة، ولو أسقطه المحلِّل عادت
الخطّة إلى سلوكها الأصليّ.

**جُرِّبت الرحلة كاملةً في متصفّح**: خرج الملفّ، صيغَ ردٌّ عليه،
فقالت المعاينة «ده ردّ على طلب تحليل للذكرى دي»، وكُتب فيها بلا نسخةٍ
ثانية، والاقتباس صار اقتباس الظهور.

**ملفّات:** `services/analysis/request.js` (جديد) ·
`services/import/parse.js` · `services/import/plan.js` ·
`views/import-view.js` · `views/scene-view.js` ·
`tests/analysis-request.test.js` (جديد، ١٨).

---

## 9.9-د ما لا يُنفَّذ في هذه الجولة

WS5-ج (الكلمات والأنماط ككيانات — يحتاج محلّلًا صرفيًّا) ·
WS7 (Drive) · WS8 (الأداء طويل المدى).

**سبب التأجيل معماريّ لا زمنيّ:** كلّها تقرأ الكيانات التي يبنيها WS1
أو تكتب فيها. بناؤها الآن يعني بناءها مرّتين.

---

## 9.10 التحقّق

**قبل الدمج:**
- `node tests/run-headless.mjs` — القائمة تبقى خضراء + الجديدة. ✅ 539/539،
  ومعها فحص الإقلاع و**فحص الشاشات** وفحص رسم الوحدات وفحص التهريب
  المزدوج.

### فحص الشاشات — ولماذا لم تكفِ الفحوص التي قبله

استعملتُ `STATE` في `scene-view` بلا استيرادها، فمرّت الخمسمئة اختبار
وفحصُ رسم الوحدات وفحصُ الإقلاع كلها:

- الاختبارات تستورد الخدمات لا الشاشات.
- فحصُ الوحدات يستورد الملفّ فيرى أعلاه ولا يرى **داخل دالّة**.
- فحصُ الإقلاع يزور `/settings` وحدها.

فصار كل مسارٍ يُفتَح ويُطالَب بألّا يرمي — ثلاث عشرة شاشة، منها
`/scene/:id` و`/day/:date` على ما زُرع للتوّ. وثلاثةٌ تعلّمتُها وأنا
أبنيه:

- **الشاشة الفارغة تُثبت القليل.** أوّل نسخةٍ مرّت على استيرادٍ محذوف،
  لأن القاعدة الفارغة ترسم حالتها الفارغة ولا تبلغ السطر المكسور.
  فنزرع أوّلًا عبر الخدمات نفسها: ذكرى وشخص وخيط ومحادثة وسكريبت
  وتعبير وكلمة محفوظة.
- **`browser.newPage()` يفتح سياقًا جديدًا في كل مرّة**، ولكل سياقٍ
  تقسيمُ تخزينٍ مستقلّ — فكانت البذرة تُكتَب حيث لا تراها شاشة. قِسْتُ
  ذلك قبل إصلاحه (`p1.context() === p2.context()` ← `false`)، فسياقٌ
  واحدٌ للبذرة وللشاشات، وعاملُ الخدمة ممنوعٌ داخله.
- **الانتظار على اختفاء الدوّارة لا على مدّةٍ مقدَّرة.** تسعمئة مليّة
  كانت تكفي الشاشات الخفيفة وتقيس شاشة الذكرى وهي نصف مرسومة.

وأُثبت أنه يبيت: بحذف استيراد `counted` من `language-view` سقط
`/language` بلوحة خطأ — بينما مرّ فحصُ الوحدات.
- **اختبار ترقية v7 على بيانات حقيقية** ✅: ثلاثة اختبارات تبني قاعدة
  v6 **بلا `eventTypes`** — أي جهازك حرفيًّا — فيها تخصيصاتٌ في
  `settings['scene.types']` ومشاهد تستعملها، ثم ترقّيها وتسأل: هل بقي
  كل شيء؟ (وواحدٌ منها يعيد تشغيل الترقية ليثبت أنها محايدة.)
- استرجاع ملفٍّ ذهبيٍّ قديم (`tests/fixtures/v1-*.llife`) على قاعدة v7.

**يدويًّا في متصفّح حقيقي:**
1. حذف سكريبت ← انتظار زوال الإشعار ← **فتح السلة ← استرجاعه**. (د-1)
2. ✅ إعادة تسمية نوع مستعمَل ← ظهور «مستعمَل في N» ← التحديث في الكل ←
   التحقّق في: المشهد، حياتي. **مُتحقَّق في متصفّح حقيقي**، ومعه ما هو
   أهمّ: الذكرى **لم تُكتَب** — `scene.type` كما هو، والاسم الجديد
   يظهر لأن الذكرى تحمل المعرّف لا الاسم. (بند 9)
3. إنشاء «فحص في الموقع» بينما «فحص موقعي» موجود ← ظهور اقتراح تشابه
   **بلا دمج تلقائي**. (بند 11)
4. مشهدان في يومين ← ربطهما بخيط ← بقاؤهما مشهدين منفصلين. (بند 27)
5. جملة المواصفة في وضع الكلمة ← الست كلمات كلّها تُقرأ. (بند 20)
6. لقطات على أربعة مقاسات: سطح مكتب، تابلت أفقي، تابلت رأسي، موبايل.
   (بندان 7 و109)

**التسليم النهائي** بصيغة البند 114: الخطة، ما تُحقِّق منه، ما أُصلح، ما
أُضيف، تغيّرات القاعدة، الترقيات، الملفّات، الاختبارات المُشغَّلة فعلًا،
المنفَّذ جزئيًّا، **غير المنفَّذ**، والقيود المعروفة. بلا تضخيم.
